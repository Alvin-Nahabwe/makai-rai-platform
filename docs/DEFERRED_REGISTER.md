# Deferred & Parked Register — MAK-AI RAI Toolkit

**Status:** Living document · Opened 2026-08-02 · Governed by `AGENTS.md` rule 6

The single audit trail for everything consciously *not* done yet. A decision to defer is a
legitimate engineering choice; losing track of it is not. This register exists so that no
deferral can be quietly dropped, forgotten, or papered over.

## Rules

1. **Nothing is ever deleted.** Rows are closed, not removed. History is the point.
2. **Every row needs a pick-up condition** — a date, an event trigger, or both. "Later" is
   not a pick-up condition.
3. **Closure requires evidence.** Per `AGENTS.md` rule 2, done means driven live. A closed
   row cites the commit and states what was verified live — and what was not.
4. **Dropping or changing a deferral is itself a recorded decision**, with justification.
   Moving something to `Dropped` without a written reason is not permitted.
5. **New deferrals enter in the same commit as the spec or change that created them.**
6. **A row opened on "cannot" grounds requires evidence of the attempt.** Distinguish *"I have
   chosen not to do this now"* (needs justification and a trigger) from *"I could not do
   this"* (a factual claim, and a hypothesis until tested). The latter must carry the command
   run and its actual output, or the source read and what it said. A register row is not a
   substitute for trying — see D-068, where a skill was declared unrunnable and given a row
   without ever being examined, and the real fix was one command. This failure mode is worse
   than a silent skip: a skipped step leaves a gap an audit finds, while a laundered deferral
   leaves a correct-looking row that passes the audit.

## Kinds

| Kind | Meaning | Closes when |
|---|---|---|
| `DEFERRAL` | Will be built, just not now | Built + verified live |
| `PARKED` | May or may not ever be done; decision itself is deferred | Decided either way, with rationale |
| `RISK` | Consciously accepted, not being mitigated now | Mitigated, or the risk materially changes |
| `BUG` | Known defect, not yet fixed | Fixed + verified live |

## Status values

`Open` · `Scheduled` · `In progress` · `Closed-done` · `Closed-dropped` · `Closed-changed`

---

## Register

| ID | Item | Kind | Opened | Why deferred | Pick-up trigger | Target | Status |
|---|---|---|---|---|---|---|---|
| D-001 | Evidence attachment + credibility tiers | DEFERRAL | 2026-08-01 | Own design surface; would dilute the Phase-1 isolation spine spec | Phase-1 spine merged + verified live | Phase 1b (own spec) | Open |
| D-002 | Review / sign-off workflow (`reviewer` role powers) | DEFERRAL | 2026-08-01 | Depends on D-001 credibility tiers | D-001 closed | Phase 2 | Open |
| D-003 | Framework as versioned data (`FrameworkVersion`, `Assessment.frameworkVersionId`) | DEFERRAL | 2026-08-01 | Own design surface (content shape, crosswalk, publish lifecycle) | Before the first real pilot assessment is created | Phase 1b | Open |
| D-004 | `reviewer` role ships inert (capabilities identical to `viewer`) | DEFERRAL | 2026-08-02 | Its distinguishing power belongs to D-002; inventing capabilities now would be speculative | D-002 picked up | Phase 2 | Open |
| D-005 | Postgres RLS fallback if the de-risking spike aborts | RISK | 2026-08-02 | Task 0 spike (`docs/superpowers/spikes/2026-08-02-rls-prisma7-findings.md`): NO-GO on `PrismaClient.$extends({ $allModels.$allOperations })` — wrapping ops in `base.$transaction(async tx => {...; return query(args)})` does not propagate the `set_config` GUC into `query(args)` (PROBE A: 0 rows, not 1; fails closed, stable under concurrency). Per ruling, RLS itself was not abandoned on that basis — Task 0b then probed the `withOrg(orgId, cb)` explicit-transaction-handle pattern (no `$extends`) standalone: all 9 probes passed (scoped read/write, cross-org read/write, nested writes, raw queries, 10-way concurrency with zero leaks across 3 runs, and pool behaviour — 20 concurrent calls on a 5-connection pool queued cleanly, 0 rejections/timeouts, ~430ms). Verdict: GO for `withOrg`, NO-GO for `$extends`. Tasks 3 and 4 as originally scoped (assuming `$extends`) are still dropped; Task 3 must be redesigned around `withOrg`, which touches every call site (no transparent wrapper) **CLOSED at the Plan 1a exit, 2026-08-03.** The pick-up condition — the data-layer redesign around the verified `withOrg` mechanism — is discharged: `withOrg` ships in `lib/data/tenant.ts` (commit 9ba0248), RLS policies and FORCE ship on all seven tables (accfa3a→`accf33a`), and the end-to-end isolation proof ships in `__tests__/integration/isolation.test.ts` (b015d27), verified live on a real `makrai_app` connection. The row's two residuals are not lost, they are re-homed: API-ergonomics cost across every call site is D-072 (9 routes still on the superuser connection) plus D-074 (the 22-entry ESLint allowlist), and real-workload pool sizing is D-065. Note the row's own title is now misleading — the spike did not abort; it returned GO for `withOrg` and NO-GO for `$extends`, and that verdict held through implementation. | Task 3 data-layer redesign around the verified `withOrg` mechanism — scope the API-ergonomics cost (every call site becomes `withOrg(orgId, tx => ...)`) and real-workload pool sizing (spike used illustrative pool=5/burst=20, not a production traffic model) | Phase 1b — before RLS-dependent tasks resume | Closed-done |
| D-006 | `/admin/assessments` cross-tenant listing removed | PARKED | 2026-08-02 | Existed only because the app was single-tenant; "vendor reads all your RAI evidence" is indefensible for an assurance tool | A concrete platform-support need arises that cannot be met otherwise | — | Open |
| D-007 | `/api/research/export` gated off | DEFERRAL | 2026-08-02 | Consent is per-user today; org-level research agreements + individual opt-out (D-008) not yet designed | D-008 closed | Phase 3 | Open |
| D-008 | Consent model: org-level research agreements with individual opt-out | DEFERRAL | 2026-07-31 | Ethics-bearing design; must not be bundled into ToS (coerced consent indefensible for an RAI tool) | Before any cross-institutional research use | Phase 3 | Open |
| D-009 | Research-data curation pass (what questions, what stratification variables) | DEFERRAL | 2026-07-31 | Needs a deliberate research-data-model note; collect progressively to avoid onboarding friction | Alongside D-008 | Phase 3 | Open |
| D-010 | Benchmarking / `BenchmarkAggregate` + minimum-cohort (k) threshold | DEFERRAL | 2026-08-02 | Requires ≥3 tenants to be meaningful; k-threshold must exist before the first aggregate query ships | First aggregate/benchmark query is written | Phase 3 | Open |
| D-011 | Per-org framework customization | PARKED | 2026-07-31 | Phase 1 uses one global published version; customization is a governance surface of its own | Institution requests contextualization | Phase 3+ | Open |
| D-012 | Quick Check assessment mode parked/removed | PARKED | 2026-07-31 | Unevidenced self-scoring runs counter to the evidence-first vision; redundant second flow. Code remains in git history | Evidence emerges that a short on-ramp drives adoption | — | Open |
| D-013 | Knowledge Bank / Resources tab | DEFERRAL | 2026-07-31 | Planned feature; `FrameworkVersion` content model treats controls/references/standards as first-class now so it renders clean later | D-003 closed | Phase 2+ | Open |
| D-014 | Feedback / bug-reporting feature (form + admin view) | DEFERRAL | 2026-07-31 | Not needed before real users exist | First real-user contact (pilot) | Pilot | Open |
| D-015 | Dedicated-instance tier (escape hatch for physical separation) | RISK | 2026-08-02 | Designed on paper only; shared-DB co-mingling is in tension with the Malabo Convention framing the product itself cites | Any procurement/residency question from a ministry or funder | — | Open |
| D-016 | Soft-delete purge job for `Organization.deletedAt` | DEFERRAL | 2026-08-02 | Soft-delete lands in Phase 1; the purge/retention job is separable | Before production data retention matters | Phase 2 | Open |
| D-017 | Noisy-neighbour controls (read replica, `statement_timeout`, split pools) | DEFERRAL | 2026-08-02 | Premature before real multi-tenant load | Second active tenant, or first slow-query incident | Phase 3 | Open |
| D-018 | Hosting / deployment model | PARKED | 2026-07-19 | Deliberately not a design input until raised; also gates the cloud-provider skills tier | Raised by the user, or first deploy need | — | Open |
| D-019 | Which artifact to generate first (model card vs datasheet) | PARKED | 2026-07-19 | Sequencing decision for Phase 2 | Phase 2 start | Phase 2 | Open |
| D-020 | Scoring methodology (maturity-level rubric replacing the opaque percentage) | PARKED | 2026-07-19 | Needs domain content work; an irreversible fork requiring `what-if-oracle` | Phase 2 start, or first institutional decision based on a score | Phase 2 | Open |
| D-021 | B2 — `/terms` and `/privacy` 404 while registration *requires* accepting them | BUG | 2026-07-31 | **STALE ON ENTRY (audited 2026-08-02):** both `app/(public)/terms` and `app/(public)/privacy` exist in code. Transcribed from PHASE_0_FINDINGS without checking. Original reason: Phase-0 harvest, absorbed into Phase 1 | Phase 1 onboarding work (registration is being rewritten) | Phase 1 | Open (needs live verification) |
| D-022 | B3 — `/forgot-password` 404, linked from every login page | BUG | 2026-07-31 | Phase-0 harvest; email path (`resend`) present but unproven **Re-targeted at the Plan 1a exit, 2026-08-03, with justification (§1 C6 forbids exiting a phase with an open row targeted at it, so this is an explicit decision rather than a silent carry).** The target read "Phase 1", which spans 1a and 1b; this row's own pick-up trigger is auth work, and Plan 1a — an isolation spine of schema, RLS, a data-access layer and its guards — did none of that work. Nothing in this row was addressed, deferred, or re-scoped by Plan 1a; only its target is made precise. | Phase 1 auth work | Phase 1b | Open |
| D-023 | B4 — dark-mode `.assessment-header` renders light-on-white | BUG | 2026-07-31 | Cosmetic; batched with UI work **Re-targeted at the Plan 1a exit, 2026-08-03, with justification (§1 C6 forbids exiting a phase with an open row targeted at it, so this is an explicit decision rather than a silent carry).** The target read "Phase 1", which spans 1a and 1b; this row's own pick-up trigger is a UI pass, and Plan 1a — an isolation spine of schema, RLS, a data-access layer and its guards — did none of that work. Nothing in this row was addressed, deferred, or re-scoped by Plan 1a; only its target is made precise. | Phase 1 UI pass | Phase 1b | Open |
| D-024 | B5 — report "Maturity Levels" legend shows all four tiers as (0–24%) | BUG | 2026-07-31 | **STALE ON ENTRY (audited 2026-08-02):** `app/(authenticated)/assessment/[id]/report/page.tsx:170` renders 75-100/50-74/25-49/0-24 correctly. Transcribed without checking | Phase 1 report port | Phase 1 | Open (needs live verification) |
| D-025 | B6 — sidebar nav text low-contrast in dark mode | BUG | 2026-07-31 | Cosmetic; batched with UI work **Re-targeted at the Plan 1a exit, 2026-08-03, with justification (§1 C6 forbids exiting a phase with an open row targeted at it, so this is an explicit decision rather than a silent carry).** The target read "Phase 1", which spans 1a and 1b; this row's own pick-up trigger is a UI pass, and Plan 1a — an isolation spine of schema, RLS, a data-access layer and its guards — did none of that work. Nothing in this row was addressed, deferred, or re-scoped by Plan 1a; only its target is made precise. | Phase 1 UI pass | Phase 1b | Open |
| D-026 | B7 — hydration mismatch from the no-flash theme script | BUG | 2026-07-31 | **STALE ON ENTRY (audited 2026-08-02):** `suppressHydrationWarning` is present at `app/layout.tsx:32`. Transcribed without checking | Phase 1 shell rewrite | Phase 1 | Open (needs live verification) |
| D-027 | B8 — `middleware` file convention deprecated in Next 16 (wants `proxy`) | BUG | 2026-07-31 | **ALREADY FIXED (audited 2026-08-02):** `proxy.ts` exists at repo root, landed in `baf2239`; its rate-limit headers are asserted by a passing e2e test, so the file is live. Transcribed as Open without checking | Phase 1 active-org routing work | Phase 1 | Closed-done |
| D-028 | B9 — PDF output thin (~3.4 KB); report depth in PDF unreviewed | BUG | 2026-07-31 | Content-depth question, not a crash **Re-targeted at the Plan 1a exit, 2026-08-03, with justification (§1 C6 forbids exiting a phase with an open row targeted at it, so this is an explicit decision rather than a silent carry).** The target read "Phase 1", which spans 1a and 1b; this row's own pick-up trigger is the report/PDF port, and Plan 1a — an isolation spine of schema, RLS, a data-access layer and its guards — did none of that work. Nothing in this row was addressed, deferred, or re-scoped by Plan 1a; only its target is made precise. | Phase 1 report/PDF port | Phase 1b | Open |
| D-029 | Org `slug` immutable in Phase 1 (no rename) | PARKED | 2026-08-02 | Renaming breaks every shared link and bookmark; doing it properly needs old-slug redirect records | An org asks to rename | Phase 2 | Open |
| D-030 | Copy-link invite fallback if email delivery fails live | RISK | 2026-08-02 | `resend` is installed but no email has ever been sent live in this app (see D-022); invitations would be its second unproven consumer | Email path fails live verification during Phase 1 | Phase 1, conditional | Open |
| D-031 | **Weight & threshold provenance** — 78 question weights (0.05–0.20) and uniform 75/50/25 principle thresholds have *no* recorded derivation | RISK | 2026-08-02 | Discovered 2026-08-02: the union of all question keys contains no `source`/`citation`/`rationale` field, and `questionBank.meta` holds only version/count/minutes. The numeric layer driving every score is untraceable — the tool's own PO-07 / IP-04 failure | Before any score is shown to an external institution; blocks D-020 | Phase 2 | Open |
| D-032 | Content currency audit vs NIST SP 1270 / UNESCO / EU AI Act | DEFERRAL | 2026-08-02 | Framework structure is plausibly research-grounded but has never been re-verified; the EU AI Act has moved since inception | Runs with D-003 (content becomes versioned data) | Phase 1b | Open |
| D-033 | ISO 42001 / AU Continental Strategy / UNESCO EIA crosswalk absent from content | DEFERRAL | 2026-08-02 | No crosswalk field exists anywhere in the content files, yet §3.2 promises output "audit-ready against ISO 42001". Not a bug — a missing dimension | Runs with D-003; required before the Phase 4 crosswalk claim | Phase 1b | Open |
| D-034 | Harvest classification of engine mechanics (staging, gating, cross-stage propagation) | DEFERRAL | 2026-08-02 | Proposed ADAPT: machinery is behaviourally proven but must serve versioned frameworks and org context **Re-targeted at the Plan 1a exit, 2026-08-03, with justification (§1 C6 forbids exiting a phase with an open row targeted at it, so this is an explicit decision rather than a silent carry).** The target read "Phase 1", which spans 1a and 1b; this row's own pick-up trigger is the engine ports (Section 4 gate), and Plan 1a — an isolation spine of schema, RLS, a data-access layer and its guards — did none of that work. Nothing in this row was addressed, deferred, or re-scoped by Plan 1a; only its target is made precise. | Before the engine ports (Section 4 gate) | Phase 1b | Open |
| D-035 | Harvest classification of report + PDF | DEFERRAL | 2026-08-02 | Proposed ADAPT: built to report one anonymous assessment; must carry org context, evidence links, credibility tier, longitudinal comparison **Re-targeted at the Plan 1a exit, 2026-08-03, with justification (§1 C6 forbids exiting a phase with an open row targeted at it, so this is an explicit decision rather than a silent carry).** The target read "Phase 1", which spans 1a and 1b; this row's own pick-up trigger is the report ports (Section 4 gate), and Plan 1a — an isolation spine of schema, RLS, a data-access layer and its guards — did none of that work. Nothing in this row was addressed, deferred, or re-scoped by Plan 1a; only its target is made precise. | Before the report ports (Section 4 gate) | Phase 1b | Open |
| D-036 | Harvest-vs-redesign decision was made without the mandated `what-if-oracle` | RISK | 2026-08-02 | `SKILLS_INVENTORY.md` names it an irreversible fork requiring the oracle; `PHASE_0_FINDINGS.md` §3 shows no oracle run. Accepted for now because the classification gate (VISION §4.3) delivers the substantive value the oracle would have | Re-run if the classification work suggests the evolve-don't-restart call was wrong | — | Open |
| D-037 | Score renders with a **provisional** marker in UI + PDF; Phase-1 output not presented to any external institution as authoritative | RISK | 2026-08-02 | Phase 1 needs a runnable assessment for its live-verification exit criterion, but the scoring's derivation does not exist (D-031) and the methodology is scheduled for re-derivation (D-020). Marking it is the honest middle path between blocking Phase 1 and shipping an untraceable number unmarked | D-020 **and** D-031 both closed → remove the marker | Phase 2 | Open |
| D-038 | Migration backfill is dev-only; no production-data migration has been rehearsed | RISK | 2026-08-02 | No production deployment exists (D-018), so the expand→backfill→constrain path is exercised against dev data only | First real deployment | Phase 2 | Open |
| D-039 | Engine unit tests validate behaviour against themselves, not weight provenance | RISK | 2026-08-02 | 83 passing tests are real evidence the engine does what it was built to do, and no evidence that what it was built to do is correct. Keeping those claims separate is the point of the harvest gate | Closes with D-031 | Phase 2 | Open |
| D-040 | Production connection-pool sizing for `withOrg` interactive transactions | RISK | 2026-08-02 | Every request holds a transaction for its duration. Spike showed 20 concurrent calls / pool of 5 queue cleanly (~430 ms, no timeouts) but used a synthetic 100 ms hold, not a production traffic model. Prisma `timeout`/`maxWait` defaults become load-bearing and must be pinned explicitly (ADR-0001) | First load test, or hosting decision (D-018) | Phase 2 | Open |
| D-041 | Non-tenant data path (`User`, `ConsentRecord`) needs its own documented boundary | DEFERRAL | 2026-08-02 | Enumerating call sites found 17 of 50 are NOT tenant data (`user` ×14, `consentRecord` ×3). Login reads `User` before any org context exists, so `withOrg` structurally cannot serve them. `lib/data/identity.ts` per ADR-0001 | Plan 1a Task 6 (data layer) | Phase 1 | Closed-done |
| D-042 | Junk / abandoned organizations from self-serve creation | PARKED | 2026-08-02 | Raised during the onboarding decision (cold registration creates an org) and **not recorded at the time — a rule-6 miss caught by the product owner**. Registration-gating is a policy concern separable from the tenancy invariant | First pilot cohort, or evidence of abandoned orgs accumulating | Pilot | Open |
| D-043 | Two-factor authentication | DEFERRAL | 2026-08-02 | Not implemented; credentials-only today. RAI governance evidence is reputationally sensitive, so 2FA is likely an institutional expectation rather than a nicety | Plan 1b auth rewrite decision, or first institutional security questionnaire | Phase 1b decision | Open |
| D-044 | SSO / OAuth provider implementation | DEFERRAL | 2026-08-02 | **Decided 2026-08-02 (ADR-0002), not open-ended:** membership is never granted by authentication (no domain auto-join); credentials stay on `User` for Phase 1b; a provider ships when a real institution names one, so we build against a known protocol (OIDC vs SAML is a large cost difference) rather than a guessed one | An institution names its IdP, or a procurement questionnaire raises SSO | Phase 2 | Open |
| D-045 | JWT sessions cannot be revoked server-side; `maxAge` unset (30-day default) | RISK | 2026-08-02 | `session: { strategy: 'jwt' }` at `lib/auth.ts:110` with no `maxAge`. Logout discards the client token only — a stolen token stays valid up to 30 days. Partly mitigated by the Phase-1 decision to read membership+role per request rather than from the token | Plan 1b auth rewrite — pin `maxAge` at minimum; revisit strategy if revocation is required | Phase 1b | Open |
| D-046 | Rate-limit store is in-memory; breaks under multi-instance hosting | RISK | 2026-08-02 | `proxy.ts` + `lib/rate-limit.ts` work correctly single-instance, but the store is module-level: it resets on restart and does not coordinate across instances. A horizontally scaled deploy silently multiplies every limit by the instance count | Hosting decision (D-018) or first multi-instance deploy | Phase 2 | Open |
| D-047 | Account and organization inactivity strategy | DEFERRAL | 2026-08-02 | Only admin-driven `isActive` exists. No automatic account expiry, no idle-session timeout, nothing for dormant orgs holding tenant data | Alongside D-045 (session policy) | Phase 2 | Open |
| D-048 | Password recovery is a non-functional stub | BUG | 2026-08-02 | Corrects D-022, whose "404" description was wrong. `app/(public)/forgot-password/page.tsx` renders but has no form submission and there is no reset API route. Also blocked on the never-exercised email path (D-030) | Plan 1b auth rewrite | Phase 1b | Open |
| D-049 | UI/UX and design system never given a harvest classification | DEFERRAL | 2026-08-02 | Raised by the product owner. VISION §4.3 lists "report/assessment UI components" as harvest candidates; only report+PDF was classified (D-035). The shell, navigation, forms, design tokens and accessibility posture were never subjected to the ADOPT/ADAPT/RE-DERIVE/RETIRE gate — inherited from SPA-era design thinking for a different product | Before any UI ports in Plan 1b | Phase 1b | Open |
| D-050 | `docs/ui-ux-audit.md` (2026-07-02, 27 KB) never read or factored into any decision | RISK | 2026-08-02 | An existing audit of the thing D-049 says needs auditing, unexamined. May already answer part of D-049, or be stale like the Phase-0 bug rows | Read it at D-049 pick-up; verify its findings against current code before trusting any of them | Phase 1b | Open |
| D-051 | API hardening gaps: input-validation audit, CSRF stance, API versioning | DEFERRAL | 2026-08-02 | All 12 non-auth routes do authenticate and rate limiting is enforced, but `lib/validate.ts` has never been audited, no CSRF position is documented, and Phase 4's public API has no versioning strategy | Plan 1b route port (validation/CSRF); Phase 4 (versioning) | Phase 1b / Phase 4 | Open |
| D-052 | Remaining inherited register rows still unverified | RISK | 2026-08-02 | **Four of eight Phase-0 bug rows (D-021, D-024, D-026, D-027) were stale when written** — transcribed from `PHASE_0_FINDINGS.md` without checking the code. D-023, D-025, D-028 remain unverified. The register is a decision input, so unverified rows are actively misleading **Re-targeted at the Plan 1a exit, 2026-08-03, and this exit produced strong evidence FOR the row's premise.** Task 7 verified eight inherited rows against running code (D-005, D-055–D-062). **Four did not survive contact:** D-055 described an `assertIsTestDatabase()` suffix check that does not exist (the code does a strict identity comparison); D-056 named a migration that was discarded in the rollback and a `DELETE` statement its replacement does not contain; D-058 and D-059 duplicated newer rows, and D-059 additionally asserted a lint state (three `no-explicit-any` errors) that is false today. So the hit rate on unverified inherited rows was **50%**, which is the concrete argument for doing this per-phase rather than trusting the register's text. The remaining inherited rows are still unverified and are Plan 1b's to check before their target phases begin. | Verify each against running code before its target phase begins | Phase 1b | Open |
| D-053 | Identity/account table + linking implementation | DEFERRAL | 2026-08-02 | `what-if-oracle` found the irreversible part is the ACCOUNTS, not the schema: a table is a mechanical afternoon migration (backfill `provider='credentials'`), but a population of real accounts with undecided linking semantics forces retroactive reconciliation, and every reconciliation is a potential takeover. Linking policy therefore recorded NOW in ADR-0002; the table waits. Also avoids committing our identity model to `next-auth@5.0.0-beta.31`'s adapter schema | **Before the first real (non-dev) account exists** — an event, not a phase. Retrofit stops being mechanical after this point | Before pilot | Open |
| D-054 | Shared-device / low-connectivity access patterns | RISK | 2026-08-02 | Surfaced by the ADR-0002 oracle's Wild Card branch (10%): in the target context (African university computer labs) shared machines and intermittent connectivity may matter more than federation. Makes D-045 materially worse — a 30-day non-revocable JWT on a shared lab machine is a different risk than on a personal laptop. Also threatens email-dependent flows (invitations D-030, password recovery D-048) | Pilot site assessment, or first report of shared-device use | Pilot | Open |
| D-055 | `resetDb()` guard uses a `_test` **suffix** convention, not a strict identity check | RISK | 2026-08-02 | Raised by the Task-1 fix re-reviewer. `assertIsTestDatabase()` accepts any database whose name ends in `_test`, so a real database named e.g. `staging_test` would pass the guard and be truncated. Accepted for now: the guard closes the confirmed hazard (`.env` points at the dev database `makrai`) and a suffix convention is proportionate while the only databases are `makrai` and `makrai_test` **CLOSED 2026-08-03 — the redo did not reproduce the condition, verified rather than assumed.** `assertIsTestDatabase()` does not exist in the redo. `__tests__/helpers/db.ts:17` performs a strict identity check, `if (db !== 'makrai_test') throw`, against the value of `SELECT current_database()` — not a `_test` suffix convention, and not a check on the URL string. A database named `staging_test` would be refused. Closed as not-reproduced rather than fixed, because there was no code path to fix. | A second environment is introduced, or any database is created whose name ends in `_test` but is not disposable | Phase 2 | Closed-done |
| D-057 | `fileParallelism: false` is process-local; CI sharding would resurface the flake | RISK | 2026-08-02 | Raised by the Task-3 reviewer. The fix serialises test files **within one vitest process**. If CI ever runs `--shard` or a matrix across machines against the same `makrai_test` database, the TRUNCATE-vs-concurrent-write race returns and the flag looks like protection that is not there **Re-anchored at the Plan 1a exit, 2026-08-03 — still Open, and its blast radius grew.** `vitest.config.ts` still sets `fileParallelism: false`, and the redo added two more integration files that call `resetDb()` in `beforeEach` (`tenant-layer`, `isolation`, `data-boundary`), so the setting is now load-bearing for **six** files rather than the original four. `__tests__/integration/isolation.test.ts` also opens a **second** connection pool inside the vitest process (the `makrai_app` pool via `lib/data/tenant`, alongside `testDb`'s owner pool), so a future CI shard would contend on connections as well as on TRUNCATE. Nothing has fixed this; the row is carried forward deliberately, with a larger radius than when it was written. | CI introduces shard/matrix parallelism for the suite — then give each worker its own database or schema | Phase 2 | Open |
| D-058 | `tsc --noEmit` has 3 known errors: unported routes do not supply `orgId` | RISK | 2026-08-02 | Direct and intended consequence of Task 2's `NOT NULL orgId`: generated Prisma types now require `orgId` on create, and `app/api/projects/route.ts`, `app/api/assessments/route.ts`, `app/api/assessments/[id]/remediation/route.ts` predate tenancy. Deliberately NOT suppressed — silencing them with a cast or default would defeat the constraint. The compiler is correctly reporting that these routes cannot say which tenant a row belongs to **CLOSED 2026-08-03 as SUPERSEDED by D-070**, not as fixed — the three errors are still present and still deliberate. D-070 is the redo's row for the same three routes and is strictly more complete: it carries the catalog verification, the explicit no-suppression ruling, and (added at Task 4's C5) the operational consequence this row never recorded — that `npm run build` fails and `npm run verify` has been red since Task 2. Two rows for one condition is how a reconciliation gets carried forward unexamined; D-070 is the surviving one. | Plan 1b route port (spec §5, steps 8-9) | Phase 1b | Closed-superseded |
| D-056 | Task-2 orgId backfill (`20260802115349_port_tenant_tables_to_org_id`) doesn't cascade the orphan-project drop to dependent rows | RISK | 2026-08-02 | Found while hand-editing the migration. The brief's backfill drops a `projects` row whose `createdById` no longer resolves to a user (`DELETE FROM "projects" WHERE "orgId" IS NULL`), but any `assessments`/`project_metadata`/`remediation_items` row still pointing at that deleted project also inherits `orgId = NULL` from it (via the parent-orgId UPDATE) and is never deleted itself — the subsequent `SET NOT NULL` on those tables would then fail. Did not occur in this run: the seeded dev data (2 users, both extant) produced zero orphans, verified live (0 rows deleted, see task-2-report.md verification #5), so the gap is real but unexercised **CLOSED 2026-08-03 — the redo did not reproduce the condition, verified rather than assumed.** The migration this row names (`20260802115349_port_tenant_tables_to_org_id`) does not exist; it was discarded in the rollback. Its replacement, `20260803034110_port_tenant_tables_to_org_id`, contains **no `DELETE` statement at all**: line 15 *assigns* orphan projects to a Legacy organization (`UPDATE "projects" SET "orgId" = '000…001' WHERE "orgId" IS NULL`) instead of dropping them, and every child derives `orgId` from its parent. With no row deletion there is no orphan cascade to miss. Closed as not-reproduced. | Any future environment where a `projects.createdById` can dangle (e.g. hard user deletion) before this migration or its equivalent is re-run, or before this migration ships against real (non-empty, non-synthetic) production data | Phase 2 | Closed-done |
| D-059 | ESLint `no-restricted-imports` ban on `@/lib/db` under `app/**` immediately flags 23 pre-existing violations | RISK | 2026-08-02 | Task 4 wires the ban per ADR-0001/Task-3 (ruling: routes must not use the unscoped Prisma client). Verified live via a probe file, as required — but the same rule also fires on every route that has not yet been ported off `lib/db` (all current API routes under `app/api/**`, ~23 import sites across ~12 files). `npm run lint` (and therefore `npm run verify`, which chains lint) was already failing before this change for an unrelated reason (3 pre-existing `no-explicit-any` errors in `__tests__/authz/policy.test.ts`, confirmed via `git stash`), so this is not a newly-broken green build — but it adds a second, larger reason `npm run lint` stays red until Plan 1b migrates each route to `withOrg`/`identityDb` **CLOSED 2026-08-03 as SUPERSEDED by D-074.** Two of this row's factual claims do not hold in the redo, which is why it is closed rather than carried: (1) it counts **23** violations across ~12 files, where the redo enumerated **22** across 22 files, and only after the ban was fixed to catch relative specifiers — the original `paths:['@/lib/db']` form matched 20, missing `lib/auth.ts` and `lib/authz.ts` which import `'./db'`; (2) it states `npm run lint` was already failing on three `no-explicit-any` errors in `__tests__/authz/policy.test.ts`, which is false in the redo — lint reports **0 errors**, and the 22-entry shrinking allowlist exists precisely to keep it that way rather than leaving a permanently-red gate. D-074 is the surviving row. | Plan 1b route port (same trigger as D-051/D-058) — closes as each route is migrated; fully closes when the last route drops its `lib/db` import | Phase 1b | Closed-superseded |
| D-060 | Neither `appClient` (`lib/data/tenant.ts`) nor `identityDb` (`lib/data/identity.ts`) has an HMR-safe singleton guard | RISK | 2026-08-02 | Raised by the Task-4 fix-round-1 reviewer. `lib/db.ts` guards its client with `globalForPrisma`/`globalThis` specifically to survive Next.js dev hot-reload without accumulating `Pool`/`PrismaClient` instances (each carries its own `pg.Pool`); the Task-4 brief's code for the two new clients (taken verbatim, per the brief's own instruction not to deviate) omits this guard. Not yet exercised because no route runs through either client in dev yet (Plan 1b work) | Plan 1b, before either client is wired into a live dev-mode route — add the same `globalForPrisma`-style guard used in `lib/db.ts` to both | Phase 1b | Closed-done |
| D-061 | Login must read `Membership` before any org context exists, but `Membership` is now unreachable via `identityDb` | DEFERRAL | 2026-08-02 | Surfaced by the Task-4 fix re-reviewer. `identityDb`'s type excludes all seven orgId-bearing models including `Membership`, per ADR-0001's rule that only `User`/`ConsentRecord` are non-tenant. But resolving a user's memberships at login — and rendering the org switcher — is inherently a before-org-context operation. Plan 1b must design that path deliberately (a login-scoped helper, not a quiet widening of the type). Not a trap: adding `Membership` back is a one-line change, so the narrowing is a checkpoint forcing deliberation rather than a wall. **Re-evaluated at Task 4 (2026-08-03) — PARTIALLY discharged, deliberately NOT closed.** The row's *read* half is now served: `lib/data/preauth.ts` exposes `membershipsForUser(userId)`, which is exactly the "login-scoped helper" this row asked for rather than a widening of `identityDb`'s type — it returns `(Membership & { org: Organization })[]` filtered to `status: 'active'` and `org.deletedAt: null`, runs on the owner connection (the sanctioned RLS bypass, ADR-0001 §4), and its existence is pinned by `__tests__/integration/preauth-surface.test.ts` so the bypass surface cannot grow silently. The *write* half is untouched and is why this stays Open: registration must create `User` **and** `Membership` atomically, which straddles the identity/tenant boundary, and `identityDb` deliberately withholds `$transaction` (the `Pick<PrismaClient,'user'\|'consentRecord'>` allowlist excludes it — verified live by tsc probe: `Property '$transaction' does not exist on type 'NonTenantClient'`). So today there is **no sanctioned path that writes a User and a Membership in one transaction**, and Plan 1b must design that crossing rather than reach for the owner client ad hoc | Plan 1b auth/session rewrite — the read half is discharged; close this row only when the **registration write path** (User + Membership in one transaction) has a designed, named home | Phase 1b | Open |
| D-062 | `organizations` table carries no RLS policy — readable cross-tenant by `makrai_app` | RISK | 2026-08-02 | Task 5 (`20260802160000_enable_rls_and_guard_trigger`) enables + forces RLS on all six `orgId`-bearing tenant tables (`projects`, `project_metadata`, `assessments`, `remediation_items`, `memberships`, `invitations`), per the brief's explicit ruling to record `organizations` here rather than protect it there. `organizations` has no `orgId` column (it *is* the tenant), so neither the event trigger's `has_org_id` check nor Task 6's T1 enumeration test can ever cover it, and slug→org resolution (login, org switcher, invite acceptance) is definitionally a before-context read that no `app.current_org_id` GUC could scope. Net effect verified against the schema (`prisma/schema.prisma:218-231`): today the table holds only `id`/`name`/`slug`/timestamps, so the exposure is "any org's name and slug are readable," not assessment data — but this has not been separately threat-modeled and the table's shape can change. **CLOSED 2026-08-03 by the Task 5 migration `20260803074244_enable_rls_and_guard_trigger`.** The deferral rested on a premise that expired: it argued no GUC could scope slug→org resolution, which is true, and concluded that therefore no policy could be installed, which does not follow. Task 4's `lib/data/preauth.ts` (written after D-062 was opened) routes *every* before-context read — `orgBySlug`, `membershipsForUser`, `invitationByToken` — through the **owner** connection, which bypasses RLS outright, so a policy on `organizations` constrains only `withOrg`/`makrai_app`, where scoping to the current org is exactly right. The table is now `ENABLE`+`FORCE` with `org_isolation` keyed on `id` (not `orgId` — it *is* the tenant). Verified live on both databases: catalog shows `organizations\|t\|t\|1`, and over a **real `makrai_app` connection** on `makrai_test`, `SELECT count(*) FROM organizations` with no GUC returned **0**, and inside a transaction with `set_config('app.current_org_id','orgA',true)` `string_agg(id)` returned **`orgA`** only, with `orgB` invisible. **What is still true and must carry forward:** neither the event trigger (which keys on the presence of an `orgId` column) nor Task 6's T1 enumeration can ever reach this table, so **T1 must special-case `organizations` by name** — otherwise the one table whose protection was hardest to get is the one no test watches | Closed. Successor obligation: **Task 6's T1 test must special-case `organizations`**; and see D-078 for the org-creation write path this policy makes necessary | Phase 1b | Closed-done |
| D-063 | Phase-1a Tasks 1–5 were built with the mandated skill process bypassed, and the bypass was not disclosed | PROCESS FAILURE | 2026-08-02 | Schema, tenant port, RBAC, data-access layer and RLS were all implemented without `database-designer` / `database-design:postgresql`, without `senior-security` / `security-review`, without `TDD`, and without `verification-before-completion` — none invoked, contrary to rule 1 and (for security) rule 3's explicit "not deferred". Two mechanisms caused it: (1) invoking `superpowers:subagent-driven-development` made its internal review loop *feel* like the process, though it checks a diff against a brief after the fact and supplies no domain expertise; (2) deferring a skill cost nothing and left no trace, because rule 6 governed deferred work but not deferred process — `security-review` was rescheduled into a session-local todo and evaporated. Compounding it, subagents were dispatched with briefs but no obligation to follow the process themselves, and left untracked files (`scratch-probe.ts`, `scratch-probe2.ts`, a complete uncommitted `__tests__/integration/isolation.test.ts`) that no review package could ever see. The failure was surfaced by the human partner, not self-reported — a rule 4 violation on top of the rule 1 one. Remediation: rolled back to `9739454` (implementation discarded, process artifacts and spike evidence retained, ADR-0001/0002 held back for re-derivation with domain skills); rules 1, 6 and new rule 9 amended in the same commit **CLOSED at the Plan 1a exit, 2026-08-03, on evidence for both halves of its pick-up condition.** (a) All four amendments are in force — verified by content rather than by number, since AGENTS.md was restructured around checkpoints on 2026-08-03 and the old numbering no longer exists: old rule 1 → §1 C1 plus the §2 mid-execution trigger table; old rule 6 → §6, including the "a register row is not a substitute for trying" clause; old rule 9 → §4 delegation, including "every dispatch carries the process" and "everything written is committed or declared"; old rule 10 → §5, including "one error message is a symptom, not a diagnosis" and "look at the thing itself". (b) The redo's ledger names invoked skills for **every** task, 0 through 6 — audited by grep, not by recollection: `database-design:postgresql` ×12, `security-review` ×12, `what-if-oracle` ×7, `test-driven-development` ×6, `senior-security` ×10 across both name forms, `verification-before-completion` ×9, plus `rigor-loop`, `senior-architect`, `writing-plans`, `brainstorming`. No task in the redo shows none, so the amendments did not fail and the enforcement mechanism does not need reconsidering on this evidence. **Honest limit:** this closes the row's stated condition, which is about skills being *invoked*. It is not evidence that invoking them is sufficient — D-082 records two fail-open defects that survived four separate skill-backed passes and were caught only by an independent C6 review. | Verify at Plan 1a exit that all four rule amendments are in force **and** that the redo's ledger shows a named skill invocation for every task; if any task in the redo again shows none, the amendments have failed and the enforcement mechanism itself must be reconsidered | Phase 1a | Closed-done |
| D-064 | `orgId` is typed `text` and all identifiers are quoted camelCase, contrary to Postgres convention | DEFERRAL | 2026-08-02 | Surfaced by `database-design:postgresql` during the Task 0 redo: the skill's guidance is `uuid` (or `bigint identity`) for ids and unquoted snake_case for identifiers, and quoted mixed-case names were the **direct cause** of the event-trigger fail-open found in the rolled-back attempt (`object_identity` quotes mixed-case, so `split_part` matched nothing). Put to `what-if-oracle` as a data-model fork per rule 1. Ruling (human partner): **keep `text` + camelCase**, and close the actual bug class instead — the contrarian branch held up, since a `text` btree index serves equality perfectly well, so the performance argument for `uuid` is weak; the two real defects were a plan/schema mismatch (a `::uuid` cast written against a `text` column) and name reconstruction, both fixable without a schema-wide refactor. Migrating now would also risk colliding with NextAuth v5's adapter tables, which expect fixed names, and would widen Phase 1a well beyond an isolation spine. **Compensating control, mandatory:** ADR-0001 states the identifier convention explicitly, and any DDL-introspecting SQL must resolve columns through catalog/OID lookups (`pg_attribute` on `obj.objid`) and never by reconstructing a name from `object_identity` | Before a **second** DDL-introspecting tool is written (audit triggers, generic tenant tooling, schema linters) — one such tool already exists and one fail-open bug has already been caused by this; or if a Prisma model is ever added without `@@map`, since the convention is unenforced | Phase 2 | Open |
| D-065 | `withOrg` holds an interactive transaction for every request; real-workload pool sizing and Prisma's transaction `timeout`/`maxWait` defaults are unproven | RISK | 2026-08-02 | The Task 0 spike (re-derived, `docs/superpowers/spikes/2026-08-02-rls-prisma7-findings.md`) established that RLS isolation only works through `withOrg`, because `$extends` binds `query(args)` to the base client and the GUC lands on a different connection. That verdict makes transaction-per-request structural rather than incidental: tenant isolation is now coupled to connection-pool behaviour. Probe G ran 10 concurrent interleaved calls with **trivial queries** and found zero cross-contamination, but that is a correctness result, not a traffic model — it says nothing about pool exhaustion, queueing latency, or what happens when a slow request holds a connection. Prisma's transaction `timeout` and `maxWait` defaults become load-bearing under this design and are currently unpinned. Compounding: behaviour behind an external pooler depends on its mode — PgBouncer **session** and **transaction** pooling both work (the interactive transaction runs on one server connection and `set_config(..., true)` is transaction-scoped), but **statement** pooling cannot hold a multi-statement transaction at all. *(Corrected 2026-08-02 by `verification-before-completion` while drafting ADR-0001: this row originally claimed transaction mode was compatible and session mode was not, which is backwards.)* That depends on the parked hosting decision | Before first production-like load, **or** as soon as hosting is decided (D-018) — whichever comes first; pin `timeout`/`maxWait` explicitly at that point rather than inheriting defaults | Phase 2 | Open |
| D-066 | ADR-0002 (identity model & account-linking policy) deliberately **not** re-derived during Plan 1a | DEFERRAL | 2026-08-02 | The approved redo sequence was "re-derive ADR-0001 and ADR-0002, then rewrite Plan 1a". Checking the dependency before doing the work: the baseline Plan 1a references ADR-0002, identity, SSO, NextAuth and login **zero** times — only the *rolled-back* plan bound ADR-0002, and that binding was added during the work that was rolled back. The superseded ADR-0002's own Context opens "Phase 1b rewrites registration, which raised…", i.e. it is Plan 1b work by its own framing, and it concluded the **schema** needs no change now, so Plan 1a's `User`/`Invitation` shapes are unaffected. Deciding login policy before the login work that gives it context is how speculative decisions get made. **The security substance is carried here so deferring costs nothing:** a provider identity must be keyed by `(provider, providerSubjectId)` and **never by email**; never auto-link a new authentication identity to an existing account on an email claim (this is the pre-account-takeover attack); linking requires proof of control of the existing account; `email_verified` is trusted only from an explicit provider allowlist and never as a sole linking basis; an unseen IdP subject creates a NEW user rather than silently merging; unlinking must never leave an account with zero authentication methods. Re-derive the full ADR in Plan 1b with `senior-security` invoked and the pre-account-takeover literature grounded externally rather than recalled | **Before the first real account exists** — i.e. before Plan 1b's registration/login rewrite ships, or before any pilot deployment (D-018), whichever comes first. The superseded ADR's own key insight sets this trigger: "the irreversibility is not in the schema, it is in the accounts" — with zero real accounts today the decision is reversible, and it stops being so the moment one exists | Phase 1b | Open |
| D-067 | `origin/HEAD` is a hand-created local ref, so `security-review` reviews the wrong diff if a real remote is ever added with a different default branch | RISK | 2026-08-02 | The built-in `security-review` skill resolves its diff through a frontmatter shell command, `git diff origin/HEAD...`. This repository has **no git remote** (`git remote -v` is empty), so the skill aborted before its body loaded. My first instinct was to record it as unrunnable and substitute `engineering-skills:senior-security` — the human partner rejected that as papering over an unexamined problem, correctly: I had not looked at the skill at all before declaring it unfixable. **Resolved instead:** `git symbolic-ref refs/remotes/origin/HEAD refs/heads/main`, verified to make `git diff origin/HEAD...` resolve to `main...HEAD` (merge-base `9739454`, the true branch diff), with `git remote -v` still empty so no phantom remote exists. Residual risk is narrow but real: this ref is *asserted*, not fetched, so if a remote is later added whose default branch is not `main`, the skill will silently review a diff against the wrong base and report clean on unreviewed code. Undo with `git symbolic-ref -d refs/remotes/origin/HEAD` **Target corrected at the Plan 1a exit, 2026-08-03.** The Target column read "Phase 1a exit" while the row's own pick-up trigger reads "on adding a real git remote (first push / first CI)" — the two disagreed, and the trigger is the correct one: nothing at a phase exit can resolve this, because the repo still has no remote (`git remote -v` is empty). Verified at the exit that the asserted ref still resolves and still produces the intended diff: `git symbolic-ref refs/remotes/origin/HEAD` → `refs/heads/main`, and the C6 `security-review` resolved 28 commits across 27 files against it. The residual risk is unchanged — a future real remote whose default branch is not `main` would silently review the wrong diff. | On adding a real git remote (first push / first CI): run `git remote set-head origin -a` to replace the asserted ref with the fetched one, then re-run `security-review` and confirm the file list matches the branch's actual changes | On first git remote | Open |
| D-068 | A register row was used to launder an unexamined problem, and the audit that exists to catch process failures would have passed it | PROCESS FAILURE | 2026-08-02 | `security-review` aborted with `fatal: ambiguous argument 'origin/HEAD...'`. Without looking at the skill, checking the repo's remote configuration, or attempting any fix, I declared it "cannot run in this repository", opened a register row saying so, and substituted a different skill. The human partner challenged it — *"is it something that cannot be resolved or is too tasking to resolve? Why are you papering over it?"* — and investigation took under two minutes: the repo has **no git remote**, so `origin/HEAD` did not resolve; one command fixed it (`git symbolic-ref refs/remotes/origin/HEAD refs/heads/main`), verified to produce the correct `main...HEAD` diff with `git remote -v` still empty. **The failure mode is structural, not a lapse of diligence:** rule 6 makes recording a deferral the virtuous-feeling action, so when something blocks you the row *feels* like compliance and substitutes for solving the problem. It is worse than a silent skip because it is camouflaged by the process — a skipped step leaves a gap an audit detects, whereas this leaves a correct-looking row that **passes** the audit. D-063's own pick-up condition would have sailed straight past D-067. **Fixed:** AGENTS.md rule 6 now distinguishes "chose not to" from "could not" and requires evidence of the attempt for the latter; new **rule 10 (Investigate before you characterise)** makes this general — one error message is a symptom not a diagnosis, effort claims are claims, read the thing itself, escalate with findings not verdicts; register rule 6 mirrors it. D-067 was rewritten from fiction ("skill cannot run") to the real residual risk (an asserted `origin/HEAD`) **CLOSED 2026-08-03 — the mechanism is in force and was exercised repeatedly rather than merely written down.** The substance survived the AGENTS.md restructure: §6 distinguishes *"I have chosen not to"* from *"I cannot"* and demands the command and its output for the latter; §5 carries the general form; §2 makes "about to write *cannot*, *too costly*, *not applicable*" an observable trigger. Fired and held at least four times after the fix, each time turning a would-be verdict into a finding: `npm run seed` "missing script" → the script lives under package.json's `prisma` key, not `scripts`; Playwright's Chrome failing to launch on a Qt platform-plugin error → not "no browser available", the chrome-devtools MCP launched fine; a subagent about to write that a non-superuser cannot install the event trigger → probed it and got `HINT: Must be superuser to create an event trigger`, now D-079; and a FATAL Turbopack panic that looked like a Task 4 regression → root-caused to a failed `npm run build` leaving `.next` inconsistent, zero panics after `rm -rf .next`. Reporting the panic as a finding would have shipped a fiction. | At Plan 1a exit, audit alongside D-063: check every register row whose justification asserts an inability, and confirm each carries the command run and its output. Any row asserting "cannot" without evidence of an attempt means rule 10 is not yet load-bearing and the enforcement needs rethinking | Phase 1a exit | Closed-done |
| D-069 | `users.lastActiveOrgId` is an unconstrained `TEXT` — an authorization bypass if Plan 1b ever trusts it to establish org context without re-checking membership | RISK | 2026-08-02 | Surfaced by the Task 1 security review (`f9757a6`). The column is deliberately a plain `String?` with no foreign key: a real FK would need a second, named `User`↔`Organization` relation for Prisma to disambiguate, and `onDelete: SetNull` would only fire on org *deletion* while the real dangling case is membership *revocation* — so the fallback path is needed either way and the FK buys ceremony rather than safety. That reasoning holds. The residual risk is not the missing FK but what consumes the column: it can name an org the user was removed from, was suspended in, or never belonged to, and nothing in the schema prevents that. **Inert today** — no code reads it, which is why this is a RISK and not a BUG. It becomes a live privilege-escalation path the moment a route uses it to select the active tenant without a membership lookup. ADR-0001 already specifies the correct behaviour (context comes from `requireOrgContext(slug, action)`, which performs the membership check and 404s on absence), so the defence is designed — but the gap between "specified" and "implemented" is exactly where the 2026-07 IDOR lived | When Plan 1b implements the org switcher / session bootstrap: assert in review that every read of `lastActiveOrgId` is followed by a membership+status check before it grants access, and add an integration test that sets `lastActiveOrgId` to a non-member org and proves access is refused | Phase 1b | Open |
| D-070 | Three routes fail `tsc --noEmit` after `Project`/`Assessment`/`RemediationItem` gained `NOT NULL orgId` — they predate tenancy and never pass `orgId` on create | DEFERRAL | 2026-08-03 | Task 2 (`prisma/schema.prisma`, migration `20260803034110_port_tenant_tables_to_org_id`) ported the tenant tables to `NOT NULL orgId` with composite same-org foreign keys, per the Task 2 brief and ADR-0001. This is the *correct*, expected consequence for routes written before any org context existed — the type system is catching exactly the gap Plan 1b exists to close. Confirmed by running `npx tsc --noEmit`: exactly 3 errors, all "Property `orgId` is missing" — `app/api/projects/route.ts:46`, `app/api/assessments/route.ts:46`, `app/api/assessments/[id]/remediation/route.ts:37`. Not suppressed (no `@ts-ignore`, no `as any`, no placeholder `orgId`) — these routes are out of scope for Task 2, which owns only the constraint layer; they are Plan 1b's route-port work, which must derive `orgId` from `requireOrgContext(slug, action)` rather than inventing a value here. Catalog-verified the migration landed on both databases: `information_schema.columns` shows six `orgId` columns `nullable=NO` (`assessments`, `invitations`, `memberships`, `project_metadata`, `projects`, `remediation_items`) and `pg_constraint` shows exactly three two-column foreign keys (`assessments_orgId_projectId_fkey`, `project_metadata_orgId_projectId_fkey`, `remediation_items_orgId_assessmentId_fkey`) on both `makrai` and `makrai_test`. Aside, verified rather than transcribed per rule 8: the Task 2 brief's environment note that "the dev database has existing rows in `projects` and `assessments` whose `orgId` is currently NULL" did not hold at execution time — `SELECT count(*)` on `makrai.users/organizations/projects/assessments/project_metadata/remediation_items` was 0 across the board, i.e. a fresh/reset dev database. The backfill block was still applied as specified (idempotent, correct for the general case, and required for `makrai_test`'s pre-Task-1 rows) **Operational consequence, added 2026-08-03 after running it rather than reasoning about it:** these are not merely `tsc --noEmit` findings. `npm run build` **fails** on the first of them (`next build` runs TypeScript and exits 1), so `npm run verify` — which chains lint → test → build — has been **red since Task 2** and stays red until Plan 1b ports these routes. The row as originally written said only "three routes fail tsc", which reads like a type-checker annoyance; the truth is the project does not build. This matters for CI (no green gate exists to protect the branch) and it falsified a premise in the Task 4 brief, which justified the ESLint allowlist by saying a bare rule "would leave verify red" — verify was already red. The allowlist remains correct for a different reason: a lint-red gate would mask *new* violations among 22 known ones. | Plan 1b route port: add `orgId` to the three creates, sourced from `requireOrgContext`'s `OrgContext.orgId`, not invented locally | Phase 1b | Open |
| D-071 | Process fixes had been purely additive prose, and prose-only rules do not fire — AGENTS.md restructured around checkpoints | PROCESS FAILURE | 2026-08-03 | The human partner asked how mid-execution concerns are handled and pointed out that issues "keep surfacing even after having seemingly been addressed". Investigation (rigor-loop, full tier) classified every failure this session against the rule that should have caught it. **Result: every rule bound to a mechanical event held; every recall-dependent rule failed.** Held — `git status --untracked-files=all` (a command with output), the same-commit register requirement (git makes omission visible), the `UserPromptSubmit` hook (fires unconditionally). Failed, every time, despite being written down for days — "verify do not transcribe" (rule 8), "design systems not components" (rule 7), "triggers are not only task-start" (rule 1). The competing hypothesis that sheer volume caused it was tested and rejected: volume predicts uniform degradation, but failures clustered entirely on the recall side. **Generative mechanism: I had been treating a mechanism problem as a knowledge problem, and every fix added more recall-dependent prose.** Four structural gaps found: (1) no lifecycle — rules were timeless obligations, so the only answerable question was "am I violating something?" rather than "what fires now?"; (2) **no checkpoint construct at all** — rule 1 was start-of-task, rules 2/4 end-of-task, 9(d) after-task, nothing in the middle, for controller or subagent; (3) change-impact/reach entirely absent — rule 7 was written for *building*, never for *modifying*, which is exactly how the Task 2 backfill shipped three locally-correct statements that did not compose; (4) rules 6, 8 and 10 were one idea written three times, added on three separate days by local patching. Also unprohibited: justifying a process decision by its outcome ("for Tasks 1–2 that was adequate"). **Remediation:** AGENTS.md fully restructured — organised by moment (§1 checkpoints C1–C6), new §2 mid-execution trigger table keyed to *observable conditions* rather than the previous pre-registration scheme (which could only catch triggers you foresaw, though an emergent trigger is by definition one you did not), new §3 change-reach obligation, rules 6/8/10 merged into §5 with a danger ranking that puts self-generated claims **above** inherited ones, new §0 governing the ruleset itself (every amendment must name the checkpoint it binds to, or be rejected), and §7.3 forbidding outcome-justified process reasoning. All ten prior rules verified mapped to successors; nothing dropped **Test result at the Plan 1a exit, 2026-08-03.** The falsifiable condition was: if **zero** §2 mid-execution triggers fire across Tasks 3–7, the checkpoint is decorative and must be reconsidered rather than reworded. Observed: **36** fired and were acted on — Task 3: 6, Task 4: 13, Task 5: 9, Task 6: 6, C6: 2 — split roughly evenly between controller and subagents, and they were load-bearing rather than ceremonial. Triggers directly produced: the discovery that the ESLint ban's `paths` form missed every relative import; that a committed migration would have set production's app-role password to a git-published literal; that the DDL guard fired on only one of the three table-creating command tags; that D-062's exclusion rationale had expired once `preauth.ts` moved before-context reads onto the owner connection; and that two of the controller's own Task 6 corrections were themselves fail-open. **The honest limit, which matters more than the number:** a non-zero count shows the checkpoint fires, not that it is sufficient. D-082 records two fail-open defects that survived every one of these passes and were caught only by an independent whole-branch review at C6 — so the correct reading is 'the mechanism works and is not enough', not 'the mechanism works'. **CLOSED at the Plan 1a exit, 2026-08-03 — the test ran and returned a result.** Threshold was zero; observed **36** §2 triggers fired and acted on across Tasks 3–7. The restructure is therefore not decorative and does not need to be reconsidered on this evidence. Closed rather than carried, because the row existed to hold a falsifiable prediction and the prediction has been settled. **What it does not license:** the count shows triggers fire, not that they suffice — D-082 records two fail-open defects that passed through every one of these checkpoints and were caught only by the independent whole-branch review at C6. The correct summary is 'the mechanism works and is not sufficient alone'. | **Falsifiable test at Plan 1a exit:** count how many §2 mid-execution triggers fired and were acted on across Tasks 3–7, by controller and subagents. If the count is **zero across five tasks**, the checkpoint is not working — the trigger conditions are either unobservable in practice or not being read — and the mechanism must be reconsidered rather than reworded. Also re-check whether the document grew again by accretion: it went 181→249 lines in this restructure, justified as new capability plus scannable tables, but that justification is a claim and should be re-tested | Phase 1a exit | Closed-done |
| D-072 | Two live authorization authorities, and RLS will **not** cover the 9 routes still using `lib/authz.ts` | RISK | 2026-08-03 | Surfaced by the Task 3 subagent (reach assessment, §2/§3) and sharpened by the controller. ADR-0001 says `lib/authz.ts` is "deleted, not extended" because its ownership premise (`assessment.userId !== user.id`) is wrong under tenancy — a colleague in the same org legitimately reads a project they did not create. It is still present and imported by **9 route files** (`app/api/{admin/users/[id]/role, assessments/[id]/complete, assessments/[id]/remediation, assessments/[id], assessments, projects/[id], projects, reports/[id]/pdf, users/me/password}/route.ts`), so `can(role, action)` from Task 3 and `lib/authz.ts` now coexist as two authorities that will drift. **The security characterisation, which had not been stated anywhere:** `lib/authz.ts:43` reads `if (!isAdmin(user) && assessment.userId !== user.id) return null`. For non-admins this is *over*-restrictive under tenancy but fail-closed. For admins it is not: `isAdmin` tests `User.role`, the **global pre-tenancy** `UserRole` enum (`admin｜assessor`), which is unrelated to `OrgRole` and not scoped to any organization — so a global admin reads any assessment in any tenant. Critically, all 9 routes import `lib/db`, which connects via `DATABASE_URL` as `makrai` — **SUPERUSER with BYPASSRLS** — so **Task 5's RLS cannot constrain them**. The isolation spine does not reach these routes until they are ported. This is pre-existing Phase-0 behaviour, not something this plan introduced, but it means "RLS shipped" must not be read as "tenant data is protected" | **Before Plan 1a is described as delivering tenant isolation** — the Plan 1a exit statement must say explicitly that 9 routes bypass RLS via the superuser connection. Closes when Plan 1b ports the last route off `lib/db`/`lib/authz.ts` (same population as D-070's 3 tsc errors and the 22-entry ESLint allowlist); `lib/authz.ts` is deleted at that point, not refactored | Phase 1b | Open |
| D-073 | `what-if-oracle` judged **not** triggered at Task 4's two design forks — recording the judgement rather than leaving it invisible | DECISION | 2026-08-03 | §2 makes the oracle mandatory at choices that are expensive to reverse, and two Task 4 choices have that flavour: inverting `NonTenantClient` from a denylist (`Omit`) to an allowlist (`Pick`), and the `makrai_app` GRANT surface (blanket DML minus `_prisma_migrations`/`users`/`consent_records`). I judge the trigger absent, on evidence rather than convenience: `lib/data/` did not exist and had **zero importers** at the moment of the decision (`ls lib/data` → no such directory), so both are one-line reversals today, and the substantive fork they descend from — RLS as the authoritative tenant filter versus app-layer filtering — was already put to the oracle and settled in ADR-0001. §6 requires a triggered-skill-not-run to be recorded; this is the weaker case of a trigger judged not to fire, and it is logged here because the SDD ledger is gitignored and would not survive an audit. If either choice acquires callers before Plan 1b revisits it, the reversibility premise expires | **When `lib/data/identity.ts` or the `makrai_app` grants acquire their first non-test caller** (Plan 1b's first ported route) — at that point re-derive whether the choice is still cheap to reverse, and run the oracle if it is not | Phase 1b | Open |
| D-074 | 22 files are exempted from the `no-restricted-imports` raw-client ban by an explicit ESLint allowlist | DEFERRAL | 2026-08-03 | Task 4 adds the structural guard (ADR-0001 control 6) banning `@/lib/db`, `**/lib/db`, `./db` and `../db` outside `lib/data/`. Enabling it bare would fail `npm run lint` on **22 pre-existing importers**, re-enumerated live at execution time rather than transcribed (`grep -rlE "from '(@/lib/db\|\.\./+lib/db\|\./db)'" app lib` → 22 files: 20 under `app/`, plus `lib/auth.ts` and `lib/authz.ts` which use `./db`). Because `npm run verify` chains lint → test → build, a bare rule would leave the gate red for the whole of Plan 1b, and a permanently-red gate trains people to ignore it. The allowlist is therefore explicit and **shrinking**, not a blanket suppression: each entry names one file, Plan 1b deletes entries one at a time as each call site moves to `withOrg`/`identityDb`, and adding a new path is a review failure. Same population as D-070's 3 tsc errors and D-072's 9 `lib/authz.ts` routes. **Implementation note carried here because it is a silent-failure class:** the 8 entries containing a Next.js dynamic segment must be written `\\[id\\]`. ESLint `files` entries are globs, and unescaped `[id]` is a picomatch character class matching the single character `i` or `d`, so it never matches a directory literally named `[id]` — observed live 2026-08-03: with plain brackets those 8 files were NOT exempted and `npm run lint` reported 8 errors; escaped, 0 | **The allowlist reaches zero entries** — at which point delete the whole override block from `eslint.config.mjs`. Regression is self-detecting: unescaping a bracket or porting a file without removing its entry both surface immediately as lint errors | Phase 1b | Open |
| D-075 | `makrai_app` has no privilege at all on `users` and `consent_records` | RISK | 2026-08-03 | Task 4's migration grants the app role blanket DML on `public`, then `REVOKE ALL` on `_prisma_migrations`, `users` and `consent_records`. Rationale: `users` holds `passwordHash` and will carry **no** RLS policy (it is non-tenant), so without the revoke a compromise of the app role could read or overwrite every credential in the system with nothing at the database layer to stop it; `_prisma_migrations` would let it rewrite migration history. Verified live on both databases (`has_table_privilege('makrai_app','users','SELECT')` → `f`, `..._prisma_migrations','UPDATE')` → `f`, `...'projects','INSERT')` → `t`). Nothing in Plan 1a reaches these through the app role — `identityDb` and `preauth` both use the owner connection. **The referential-integrity worry was tested, not assumed:** `projects.createdById` references `users(id)`, and an `INSERT INTO projects` as `makrai_app` returned `INSERT 0 1` while a direct `SELECT count(*) FROM users` as the same role returned `ERROR: permission denied for table users` — PostgreSQL runs the FK check with the constraint owner's privileges, so the revoke does not break writes. The accepted risk is the mirror image: the moment Plan 1b moves any identity read onto the app role it will fail with `permission denied`, and the temptation will be to re-grant the whole table | **When Plan 1b moves any part of the identity path off the owner connection onto `makrai_app`** — grant back column-by-column (`GRANT SELECT (id, email, name, isActive) ON users`), explicitly **excluding `passwordHash`**, never a bare `GRANT ... ON users` | Phase 1b | Open |
| D-076 | The `makrai_app` password has a well-known default and nothing enforces that production overrides it | RISK | 2026-08-03 | The migration deliberately sets **no** password (migrations are committed and run in every environment, so a literal there would publish production's app credential in git — this is the defect the first draft of Task 4 carried). The password is instead provisioned out-of-band by `scripts/provision-app-db-role.sh <db>`, which falls back to the well-known local default `app_dev_password` — matching `vitest.config.ts` — and refuses that default only when `NODE_ENV=production`. Two gaps remain, both accepted for Plan 1a: (1) the `NODE_ENV` guard is the *only* thing standing between a real deployment and a published password, and `NODE_ENV` is trivially unset or wrong in a container — a deployment that forgets it gets `app_dev_password` silently, with just a stderr line; (2) nothing runs the script automatically, so `prisma migrate deploy` alone leaves a **LOGIN role with no password set at all**, and whether that is reachable depends entirely on the server's `pg_hba.conf` auth method rather than on anything in this repo. `npm run db:provision` makes the two-step discoverable; it does not make it enforced. Not exercised in anger yet because hosting is still parked (D-018) | **When hosting is decided (D-018) or the first non-local deployment is prepared**, whichever is first: move the app-role password to the platform's secret store, make deployment fail closed if `APP_DB_PASSWORD` is unset rather than falling back, and assert in the deploy runbook that provisioning runs after every `migrate deploy` | Phase 1b | Open |
| D-077 | RLS contains a **forgotten filter**, not **injected SQL** — `makrai_app` can re-point `app.current_org_id` itself | RISK | 2026-08-03 | Bounds every isolation claim Plan 1a makes, so it is recorded rather than left implicit. The `org_isolation` policies read `current_setting('app.current_org_id', true)`, and `withOrg` (`lib/data/tenant.ts:70-73`) sets that GUC as the app role — it **must** be able to, or the mechanism cannot work. There is no Postgres facility to make a GUC writable once per transaction, so the same capability is available to anything else running on that connection. Verified live 2026-08-03 over a **real `makrai_app` connection** (`docker exec -e PGPASSWORD=… psql -U makrai_app -d makrai_test`): `SELECT set_config('app.current_org_id','orgA',true)` returned `orgA` and the subsequent `SELECT` scoped to that org — i.e. the role freely chooses its own tenant. **Consequence:** an attacker who achieves SQL injection on the tenant path is not contained by RLS; they simply set the GUC to the victim's org. RLS defends against the *omission* failure (a forgotten `where`), which is the IDOR class that motivated this rebuild — it does not defend against injection. Parameterised queries therefore remain fully load-bearing, and `withOrg` deliberately uses parameterised `set_config(...)` rather than `SET LOCAL` (which cannot be parameterised at all); `__tests__/integration/tenant-layer.test.ts:51-59` pins that a hostile orgId is stored literally. Not mitigable at the database layer with this design; recorded as an accepted, bounded risk | **Any review of query construction on the tenant path** — specifically: the first use of `$queryRawUnsafe`/`$executeRawUnsafe` inside a `withOrg` callback, or any code that builds a tenant-path SQL string by concatenation. At that point the reviewer must treat injection as an *isolation* bug, not merely an integrity one. Also re-read when ADR-0001's "RLS is the authoritative tenant filter" is quoted in any exit statement | Phase 1b | Open |
| D-078 | Creating an organization can no longer go through `withOrg` — Plan 1b needs a sanctioned before-context **write** path | DEFERRAL | 2026-08-03 | A deliberate and foreseen consequence of the D-062 fix, recorded before it is discovered as a bug. `organizations` now carries `WITH CHECK (id = NULLIF(current_setting('app.current_org_id', true), ''))`, and a *new* organization is by definition not yet the current org — so `withOrg(ctx, tx => tx.organization.create(...))` cannot succeed for any `ctx`. Verified live over a real `makrai_app` connection on `makrai_test`: the analogous cross-org insert `INSERT INTO projects (…,"orgId",…) VALUES ('pX','orgB',…)` under `app.current_org_id='orgA'` failed with `ERROR: new row violates row-level security policy for table "projects"`; the `organizations` policy has the identical shape keyed on `id`. Nothing is broken today — no code path creates an organization outside tests, and the tests create orgs through `testDb`, which connects as the superuser owner (`vitest.config.ts` `DATABASE_URL`), so all 182 tests pass unchanged. This is a *forced checkpoint*, the same device as D-061: org creation is a before-context write and must be designed as one, in `lib/data/preauth.ts` alongside the before-context reads, rather than discovered mid-feature and "fixed" by weakening or dropping the `organizations` policy — which would silently re-open D-062 | **The first feature that creates an organization** (registration-with-new-org, or an admin "create organization" action in Plan 1b). Add the write to `lib/data/preauth.ts` on the owner connection and extend `__tests__/integration/preauth-surface.test.ts`'s export pin, which will otherwise fail and force the decision. **Explicitly forbidden as the fix:** removing `WITH CHECK` from `organizations`, or routing org creation through `withOrg` with a self-referential GUC | Phase 1b | Open |
| D-079 | This migration is **superuser-only** to apply — `CREATE EVENT TRIGGER` cannot be run by a non-superuser | RISK | 2026-08-03 | Found during Task 5 execution, not present in the plan. ADR-0001 control 4 (the DDL guard) is implemented as an event trigger, and PostgreSQL restricts `CREATE EVENT TRIGGER` to superusers with no `GRANT`able alternative. Verified live rather than asserted — attempted as the app role: `docker exec -e PGPASSWORD=app_dev_password -i docker-postgres-1 psql -U makrai_app -d makrai_test` running `CREATE EVENT TRIGGER probe_trg ON ddl_command_end EXECUTE FUNCTION probe_et();` returned `ERROR: permission denied to create event trigger "probe_trg"` / `HINT: Must be superuser to create an event trigger.` (the preceding `CREATE FUNCTION` had already failed with `permission denied for schema public`, so no residue was left; confirmed by `SELECT proname FROM pg_proc WHERE proname='probe_et'` → 0 rows). Locally this is invisible: `makrai` is a superuser and `prisma migrate deploy` succeeded on both databases. It becomes a real constraint on any managed Postgres that withholds superuser — several managed providers do — where this migration would fail outright, or would have to be applied out-of-band by an operator. Accepted for Plan 1a because hosting is parked (D-018) and the local topology satisfies it; recorded because the alternative is discovering it during a first deploy, when the tempting fix is to delete the guard | **When hosting is decided (D-018) or the first non-local deployment is prepared**, whichever is first: confirm the target offers a superuser-or-equivalent role for migrations. If it does not, do **not** drop the event trigger — Task 6's T1 enumeration test is the compensating control and would then become the *only* one, which must be stated explicitly in the exit statement rather than assumed | Phase 1b | Open |
| D-080 | Prisma silently DROPS a `where` key whose value is `undefined`, and nothing project-wide prevents it | RISK | 2026-08-03 | Found by the C6 whole-branch `security-review` and reproduced live before fixing: `preauth.invitationByToken(undefined)` returned a **live pending invitation including its token and an `owner` role**; `membershipsForUser(undefined)` returned the platform-wide membership graph; `orgBySlug(undefined)` returned an arbitrary tenant. Cause is `strictUndefinedChecks` being a Prisma preview feature that `prisma/schema.prisma` does not enable, so `undefined` elides the filter instead of raising. The declared parameter types say `string`, which does not help: callers are route handlers reading `await req.json()`, typed `any`. **Fixed at the module boundary** in `lib/data/preauth.ts` (all three functions validate the key and return `null`/`[]`), pinned by 5 tests in `__tests__/integration/data-boundary.test.ts`, and proven non-vacuous — reverting the fix turns 3 of them red. What is NOT fixed is the general case: every other Prisma call site in the codebase has the same exposure, and the systemic fix (`previewFeatures = ["strictUndefinedChecks"]`) was deliberately not applied here because it changes behaviour for the whole codebase including the 9 unported superuser routes (D-072), which is not a change to make unreviewed at a phase exit | **Before Plan 1b wires its first route that passes request-derived values into a Prisma `where`** — evaluate enabling `strictUndefinedChecks` globally, with the 9 unported routes re-tested, rather than adding per-function guards forever | Phase 1b | Open |
| D-081 | `identityDb`'s type boundary did not stop Prisma relation traversal, and its own comment claimed it did | BUG | 2026-08-03 | Found by the C6 whole-branch `security-review`. `Pick<PrismaClient,'user'\|'consentRecord'>` closes `$queryRaw*`, `$executeRaw*`, `$transaction` and future models, but hands over the **entire** `user` delegate — and `User` has relations to five orgId-bearing models. Reproduced live: `identityDb.user.findMany({ include: { memberships: { include: { org: true } }, projects: true, sentInvitations: true } })` type-checks, passes lint (`lib/data/**` is exempt from the import ban), passes every RLS test because it never touches `makrai_app`, and returns other organizations' projects, the full membership graph and live invitation tokens over the SUPERUSER/BYPASSRLS owner connection. The nested-write form (`data.memberships.updateMany`) performed an unauthorized cross-tenant role change with `can()` never consulted. **The docblock asserted the boundary was closed**, which is worse than silence — a reviewer trusts it. Fixed with a runtime guard (`assertNoTenantRelation`, applied via `$extends` argument inspection only — the Task-0 `$extends` NO-GO concerned connection state and does not apply) plus a corrected comment; 4 tests pin it, proven non-vacuous. Residual: `TENANT_RELATIONS` is a **literal list**, so a tenant relation added to `User` in Plan 1b widens the bypass unless the list is updated **Reopened and re-fixed 2026-08-03 after an adversarial review BLOCKED the first two fixes.** The Proxy that replaced the type-only narrowing was itself escaped: `identityDb.user.$parent` is an own property of Prisma's delegate pointing back at the **unproxied** client, and the reviewer reproduced `.project.findMany()`, `.$queryRawUnsafe('SELECT … FROM projects')`, `.$transaction(tx => tx.invitation.findMany())` returning a live `owner` invitation token, and a cross-org `membership.create` granting itself `owner` in a victim organization. Symbol keys skipped the trap outright (`typeof prop === 'string'`). Separately, `_count: true` — the shorthand, with no child keys for the recursive walk to reach — expanded to every relation and leaked cross-org project/membership/invitation counts as a per-user oracle. **Now fixed by CONSTRUCTION rather than interception**: `identityDb` is a frozen object built from an explicit operation allowlist, so `$parent` is absent because nothing copies it, not because something denies it. Verified after the fix: `Object.keys` is exactly `['$connect','$disconnect','consentRecord','user']`, `getOwnPropertySymbols` is empty, `user.$parent` is `undefined`, both `_count` forms are rejected, legitimate identity counts still work, and `$connect`/`util.inspect`/spread/`JSON.stringify` — all of which the Proxy had broken — work again. | **Whenever a relation is added to `User` or `ConsentRecord`** — add it to `TENANT_RELATIONS` in `lib/data/identity.ts` in the same commit. Also re-evaluate whether a hand-written delegate interface (no `include`) should replace the guard once Plan 1b knows which operations identity actually needs | Phase 1b | Open |
| D-082 | Two fail-open defects reached C6 because the per-task threat passes did not model the ORM's own semantics | RISK | 2026-08-03 | Process finding, recorded because it predicts the next failure rather than describing the last one. D-080 and D-081 were both introduced in Task 4, survived a fresh `senior-security` STRIDE pass at Task 4's C1, a subagent implementation, a controller C4 review, and Task 5's and Task 6's passes — and were caught only by the independent C6 whole-branch review. Both share a shape the threat passes never covered: **the danger was in what the ORM does with a well-formed call**, not in SQL, roles, policies or connection identity, which is where every earlier pass looked. The Task 4 C1 pass explicitly enumerated `$queryRaw`/`$transaction`/future-models as the holes in `Pick` and stopped there, treating that enumeration as complete. Also: the independent reviewer was worth more than the controller re-reading its own work, which is the self-enhancement bias `rigor-loop` names | **At the C1 of any task that adds or changes a data-access module**, add an explicit question to the threat derivation: "what does the ORM do with `undefined`, with a relation include, and with a nested write, on this path?" — and prefer an independent reviewer over self-review for any module that bypasses RLS | Phase 1b | Open |
| D-083 | `code-review` and `code-simplifier`/`simplify` were invoked **zero** times across the whole Plan 1a redo, though both are Tier 1 always-on | PROCESS FAILURE | 2026-08-03 | Found by the Plan 1a exit skill audit, measured rather than recalled: grep of the SDD ledger and all 28 branch commits returns 0 occurrences of either. This is the **same mechanism as D-063 recurring inside the redo built to prevent it** — the `subagent-driven-development` review loop *felt* like code review, but it checks a diff against a brief after the fact, which `AGENTS.md` §1 C1 explicitly says does not discharge the always-on tier. Root cause is structural, not forgetfulness: the audit found that **8 of 8 skills named in an AGENTS.md checkpoint or the `UserPromptSubmit` hook fired, and 0 of 6 named only in SKILLS_INVENTORY.md "Tier 1" did** — `code-review` was in neither. **Fixed:** C4 now names `code-review` then `simplify` explicitly, and SKILLS_INVENTORY v0.2 is restructured by moment with a maintenance rule that a §1 entry absent from AGENTS.md is decorative **CLOSED 2026-08-03 — the retroactive obligation is discharged.** `code-review` was run over the branch. Its GitHub delivery half could not run (the repo has no remote and no PR; creating one would mean publishing the codebase, which is the user's call, not the agent's), so its METHOD was run instead: five independent review lenses over the local diff — AGENTS.md/ADR adherence, a shallow bug scan, comments-versus-code informed by git history, plus `silent-failure-hunter` and `type-design-analyzer`. It was not a formality: the adherence lens returned clean, and the other four found two CRITICAL fail-opens, two HIGH ones and several MEDIUMs, all now fixed or recorded (D-086 through D-092). `simplify` was also run and produced one real change — extracting the four hand-rolled HMR singletons into `hmrSingleton`, which is where D-060's omission originally happened. | **Retroactive:** run `code-review` over the Plan 1a branch diff before it merges — it is the one always-on skill this phase never ran, and the phase is being called complete. **Ongoing:** re-audit mechanically at every phase exit (now a C6 obligation) | Phase 1a merge | Closed-done |
| D-084 | `superpowers:systematic-debugging` never invoked, through four real debugging episodes | PROCESS FAILURE | 2026-08-03 | The ledger contains exactly one mention, at Task 0, and it is a *declination* with a valid reason at the time ("no probe behaved unexpectedly"). It was then never reconsidered when the trigger actually fired: the FATAL Turbopack panics (root-caused to a stale `.next` from a failed build), Playwright's Chrome dying on a Qt platform-plugin error, `npm run seed` failing as a missing script (it lives under package.json's `prisma` key), and a subagent's psql `-c` syntax error (psql's `-c` rejects client-side `\gset`-style variables). All four were investigated to root cause and none was papered over — but §7.3 forbids defending the decision by its outcome: the debugging was ad hoc, not skill-guided, and a once-made declination silently aged, which is the §2 "reusing a previously-derived conclusion" trap. Cause: §2's row said "investigate it first" and named **no skill**. **Fixed:** the row now names the skill | **Next unexpected failure of any kind** — invoke it and record that in the ledger; if the next phase again shows zero invocations against non-zero debugging episodes, naming it in the trigger table has failed too and the binding must become mechanical rather than textual | Phase 1b | Open |
| D-085 | The most on-point tool in the inventory for Plan 1a's actual defects was never used | RISK | 2026-08-03 | `pr-review-toolkit:silent-failure-hunter` exists to find "silent failures, inadequate error handling, and inappropriate fallback behavior". **Both** defects the C6 review found (D-080 Prisma `undefined` filter-elision; D-081 `identityDb` relation traversal) are silent fail-opens on a path that bypasses RLS — precisely its target class — and it was invoked zero times, as were `engineering-skills:adversarial-reviewer` and `engineering-advanced-skills:env-secrets-manager` (the latter during Task 4's credential/provisioning work, where a committed migration would have published a production password). Two compounding causes: none was bound to a moment, and v0.1 of the inventory **listed `silent-failure-hunter` among skills when it is an Agent** (`subagent_type`), so "invoke the skill" would have failed even if someone had tried. **Fixed:** §2 now binds it to "about to write error handling, a fallback, a catch, or a default", and inventory §4 separates skills from agents from MCP tools **CLOSED 2026-08-03 — dispatched, and it earned the row.** `pr-review-toolkit:silent-failure-hunter` ran over the branch and found the two most serious defects in it: `identityDb`'s narrowing was type-only, so the runtime object still carried `project`, `$queryRawUnsafe` and `$transaction` (all confirmed working against the owner connection); and no client validated its connection string, so an unset `APP_DATABASE_URL` lets `pg` fall through to `PG*` and libpq defaults and could silently connect as the SUPERUSER owner with every control still appearing to function. It also found that T1 could not see a tenant table with RLS enabled and no policy — the exact state its own event trigger creates. `engineering-skills:adversarial-reviewer` and `engineering-advanced-skills:env-secrets-manager` were run too: the adversarial pass BLOCKED the first round of fixes and escaped them via `user.$parent`, and the secrets audit found its own tooling returns 10 false positives while missing every real committed credential, plus that the repo has no secret-scanning gate of any kind (D-093). | **Before the Plan 1a branch merges**, dispatch `silent-failure-hunter` over the branch diff — the C6 review found two fail-opens by reading the code, and this agent is purpose-built for the same class, so a second pass is cheap relative to what it is looking for | Phase 1a merge | Closed-done |
| D-086 | No connection-string validation: an unset `APP_DATABASE_URL` could silently become the **owner** connection | BUG | 2026-08-03 | Found by `silent-failure-hunter` in the C6 re-drive and reproduced by the controller. `new Pool({ connectionString: undefined })` does **not** throw — `pg/lib/connection-parameters.js` tests the value for truthiness and otherwise falls through to `PG*` env vars and libpq defaults (`host: 'localhost'`, `user: process.env.USER`). `PGUSER`/`PGPASSWORD` are conventional in containers and CI, and `.env.example` hands an operator the OWNER credential, so a missing or misspelled `APP_DATABASE_URL` could make `appClient` a SUPERUSER/BYPASSRLS connection: `withOrg` still sets the GUC, every policy still exists, every test still passes, and RLS is bypassed for every query with a correct-looking HTTP 200. **The suite structurally cannot catch this** — `vitest.config.ts` hardcodes both URLs, so it proves the test config, not the deployed one. **Fixed** by `lib/data/connection.ts` (load-time refusal on all three clients) plus a once-per-process assertion inside `withOrg` that the live connection is neither `rolsuper` nor `rolbypassrls` — the only check that verifies the architecture's central assumption against reality rather than against configuration | **When a deployment target exists**, confirm the identity assertion runs on a real connection there and is not disabled for startup performance | Phase 1b | Open |
| D-087 | T1 could not see a tenant table with RLS enabled but **no policy** — the exact state its own event trigger creates | BUG | 2026-08-03 | Found by `silent-failure-hunter`. The DDL guard enables and forces RLS on a new `orgId` table but deliberately creates no policy, and T1's predicate was `relrowsecurity = false OR relforcerowsecurity = false` — both of which the trigger has just set to **true**. Reproduced live: `CREATE TABLE "trigger_made_me"(id text primary key, "orgId" text not null)` yields `rls=true force=true policies=0` and the old T1 returned green, so a table that reads empty for every user in every organization would ship with a green suite and a `RAISE NOTICE` surfaced nowhere. The same blind spot hid a **dropped** policy: only `projects` and `memberships` were covered behaviourally, so removing `org_isolation` from `assessments` left the whole suite green while every assessment read returned zero rows. T1 was also vacuously satisfiable — `toEqual([])` passes on an empty or unmigrated schema. **Fixed**: the predicate now also fails on a missing `org_isolation` policy, and a second test asserts the protected population is exactly the six expected tables. Both proven non-vacuous | **If a seventh orgId-bearing table is added**, update the expected-population assertion in the same commit — it is intentionally a hard-coded list so that adding a table is a decision | Phase 1b | Open |
| D-088 | `withOrg`'s transaction handle still exposes `$executeRaw`, so a callback can re-scope the org GUC mid-transaction | RISK | 2026-08-03 | Found by `type-design-analyzer`; mechanism verified by the controller. Prisma 7's `ITXClientDenyList` is `["$connect","$disconnect","$on","$use","$extends"]` — lifecycle methods only — so `Prisma.TransactionClient` retains `$executeRaw`/`$queryRaw`. A callback can therefore run `set_config('app.current_org_id', <any org>, true)` and re-scope itself for the remainder of the transaction; RLS cannot prevent it, since setting a GUC is unprivileged (the same limit as D-077). **Chosen not to fix now, with reasons:** the callback is first-party code, so this is a footgun rather than an attacker path, and narrowing the type to a `Pick` over model delegates would break the tenant-layer and isolation tests, which legitimately use `tx.$queryRaw` to assert the GUC and would need a separately-provided raw handle. What *was* done: the hand-rolled `Parameters<Parameters<PrismaClient['$transaction']>[0]>[0]` — which resolved correctly only because `Parameters<>` picks the **last** overload and the interactive form happens to be last — is replaced by the generated `Prisma.TransactionClient`, and the docblock now states exactly what is and is not excluded | **When Plan 1b ports its first route to `withOrg`**, decide then whether to narrow `TenantTx` to model delegates and give the GUC tests their own handle | Phase 1b | Open |
| D-089 | Type-design items that need machinery Plan 1a does not have: unbranded `OrgContext`, `ForbiddenError`'s missing 404 sibling, `GRANTS` action-axis force, `req.json()` chokepoint | DEFERRAL | 2026-08-03 | Raised by `type-design-analyzer` in the C6 re-drive. Four related items, all deferred for the same reason — the code that would give them meaning does not exist yet, and building them now would be speculative. (1) `OrgContext` is a bare `{orgId: string; role: OrgRole}`, so `withOrg({orgId: params.slug, role: 'owner'}, …)` compiles; a `unique symbol` brand would convert seven lines of prose warning into a compiler check, but there is no `requireOrgContext` yet to mint one, so today it would only mean tests calling `unsafeOrgContext()`. (2) ADR-0001 mandates **404, never 403** for a non-member so slug probing cannot enumerate orgs, but only `ForbiddenError` exists; its 404 sibling should be defined *with* it so the status mapping is total rather than inferred from a class name by whoever writes the first error handler. (3) `GRANTS` is exhaustive on the role axis (`Record<OrgRole, …>`) but the action axis is forced only by `policy.test.ts`'s `Record<Action, …>`, and **vitest does not typecheck** — that force comes from `next build`, which is currently red (D-070); `as const satisfies` plus an `AssertNever` line would move it into production code. (4) The `preauth` `string` parameters are never checked because callers read `await req.json()`, typed `any`; a `lib/http/body.ts` returning `unknown` plus a lint rule is the only thing that makes them real. **`ForbiddenError` now carries `readonly action`/`readonly role`** — the one part with no prerequisite | **Plan 1b, at the task that creates `requireOrgContext` and the first ported route** — items 1, 2 and 4 all become buildable at that moment; item 3 is independent and can be done whenever | Phase 1b | Open |
| D-090 | `ALTER DEFAULT PRIVILEGES` silently re-widens least privilege for every future non-tenant table | RISK | 2026-08-03 | Found by `silent-failure-hunter`. The role migration grants `makrai_app` full DML on all *future* tables in `public`, while the REVOKE names only the three tables that existed (`users`, `consent_records`, `_prisma_migrations`). Every non-tenant table Plan 1b adds — sessions, password resets, an audit log — therefore gets full DML for the app role by default, inverting the migration comment's stated principle that Plan 1b grants back deliberately. Tenant tables are unaffected because the event trigger enables RLS, which denies without a policy; non-tenant tables have no such backstop. Not fixed now because changing default privileges mid-branch would silently alter what the already-applied migrations granted on both databases, and the phase is closing **CLOSED 2026-08-03, earlier than its own pick-up trigger, because a concrete exploit appeared.** The row deferred this to "the first non-tenant table Plan 1b adds", reasoning that tenant tables were safe because the event trigger enables RLS. The final C6 review found the gap that reasoning missed: `ALTER DEFAULT PRIVILEGES ... ON TABLES` also covers **views**, and the table guard never fired on one. Migration `20260803140500` revokes the default grant; verified after — a newly created view is no longer auto-granted (`has_table_privilege('makrai_app', …)` = false), and a test asserts `pg_default_acl` holds no `makrai_app` entry. | **The first non-tenant table Plan 1b adds** — either drop `ALTER DEFAULT PRIVILEGES` and grant per-table in each migration, or add a test enumerating the tables where `makrai_app` holds privileges against an explicit allowlist | Phase 1b | Closed-done |
| D-091 | The provisioning script passes the production password as an `argv` element, readable via `ps` | RISK | 2026-08-03 | Found by `silent-failure-hunter`, and it undercuts the reason the password was moved out of the migration in the first place. `scripts/provision-app-db-role.sh` invokes `docker exec … psql -v pw="$APP_DB_PASSWORD"`, so the value appears in the process table and any user on the host can read it while the command runs. The psql-side quoting (`:'pw'` via STDIN) is correct and does defend against injection — this is a separate exposure. Also noted: `docker exec -i docker-postgres-1` is hardcoded, so the `NODE_ENV=production` refusal branch is written for an environment in which the rest of the script cannot execute at all — the guard and the transport disagree about what the script is for. Not fixed now: rewriting the transport is deployment work and hosting is parked (D-018) **Second exposure channel, found by the final C6 review and not covered by this row's original text:** the script sends `ALTER ROLE makrai_app WITH PASSWORD '<literal>'` to the server, and PostgreSQL does not redact that under `log_statement=all` or in `pg_stat_statements`. So the secret reaches both the process table (argv, as recorded) *and* the server log. Fix both together when the transport is rewritten: `\password` or a client-side SCRAM hash avoids each. | **When hosting is decided (D-018)**, pass the secret via an environment variable into the container (`docker exec -e PW=…`) rather than argv, and replace the hardcoded `docker exec` with the real deployment transport | Phase 1b | Open |
| D-092 | Guards on this branch have repeatedly closed the bypass shapes their author enumerated and left the class open — four times, and three of those were fixes for the previous one | PROCESS FAILURE | 2026-08-03 | Recorded because it is the branch's dominant defect mechanism and it predicts the next one. The instances: (1) the ESLint ban caught `@/lib/db`, `../../lib/db` and `./db` and missed `../../db`, then after widening still missed `await import()` and `require()` because `no-restricted-imports` sees only static declarations; (2) the DDL-trigger pin caught the trigger's existence and all three command tags and missed `evtenabled`, so `ALTER EVENT TRIGGER … DISABLE` passed it; (3) `assertNoTenantRelation` v1 checked five named clauses at depth 1 and missed nesting, `where` combinators, `_count`, array `orderBy` and `upsert`; (4) the Proxy that replaced the type-only narrowing was escaped via `user.$parent` and symbol keys, and the recursive walk still missed `_count: true`. **In every case a test pinned exactly the shapes the author had thought of and passed.** The common root is that each guard *intercepted* — it enumerated what to deny — and an interception guard is only as good as the reachability graph its author imagined. What actually worked: building the surface by construction (nothing to escape through), deriving the denied set from generated types (`keyof Prisma.UserInclude`) so the compiler maintains it, and asserting a control's *behaviour* rather than its *presence* (T1 now checks the policy predicate, not that a policy exists). **The mechanical lesson, now in AGENTS.md C4: an independent reviewer found all four; the author's own re-reading found none of them** | **At the C1 of any task that writes a guard**: state the *class* it must close, then have someone other than the author try to escape it — `engineering-skills:adversarial-reviewer` or `pr-review-toolkit:silent-failure-hunter`, both now bound in §2. If the only evidence is a test written by the guard's author, the guard is unreviewed | Phase 1b | Open |
| D-093 | No secret-scanning gate exists anywhere in the repo, and the tool the secrets skill recommends is blind to this project's credential shape | RISK | 2026-08-03 | Found by `engineering-advanced-skills:env-secrets-manager` in the C6 re-drive. Two parts. (1) There is **no** CI workflow, pre-commit hook, husky config, gitleaks config or detect-secrets baseline in the repository — the skill's central recommendation ("enforce detection in CI before merge") has nothing to attach to, and `npm run verify` is red anyway (D-070), so no gate protects this branch at all. (2) The skill's own `env_auditor.py` returned **10 HIGH findings, every one a false positive** (a React `onChange` handler, test fixtures, documented example passwords in spec files) while missing **every** real committed credential — six tracked files carry `postgresql://user:password@host` pairs and its patterns do not match connection strings. Wiring it in as-is would produce pure noise and zero signal here. Also noted: `.env.example` contains working local credentials rather than placeholders, which the skill lists as its first common pitfall — benign today because the values are local-only, but it means copying the example yields a live config instead of a template that forces a decision | **When CI is first set up** (Phase 4, or earlier if a remote is added): add secret scanning with patterns that actually match connection strings, and verify it flags a deliberately planted `postgresql://u:p@h/db` before trusting it | Phase 4 | Open |
| D-094 | A VIEW over a tenant table bypassed RLS entirely, and every control on the branch missed it | BUG | 2026-08-03 | Found by the final C6 security review and reproduced by the controller before fixing. Three mechanisms combined: the DDL guard filters `object_type = 'table'`, so `CREATE VIEW` skipped it; `ALTER DEFAULT PRIVILEGES … ON TABLES` covers views, so `makrai_app` was auto-granted `arwd`; and because the view is owned by the SUPERUSER `makrai` and PostgreSQL's `security_invoker` defaults to **false**, the base table's RLS was evaluated with the *view owner's* rights. Measured as `makrai_app` with **no org context at all**: `SELECT count(*) FROM projects` → **0** (fails closed, correct) while the same data through a view → **2 rows across two organizations**. That inverts the invariant the whole architecture rests on, which ADR-0001 states as "forgetting to use withOrg fails CLOSED". T1 could not see it either — it filtered `relkind = 'r'`. **Fixed** by migration `20260803140500`: a second event trigger raises on any `public` view or materialized view created without `security_invoker = true`, the default privilege grant is revoked, and T1's filter widens to `('r','p','v','m')`. Pinned by two tests. **The lesson is D-092's again**: ADR-0001 control 4 claimed a forgotten policy was "structurally impossible", and it was — for the one relation kind its author had in mind | **If a reporting view, materialized view or partitioned tenant table is added**, confirm the guard still covers it; `security_invoker` is PG15+, so a downgrade below that would silently remove this control | Phase 1b | Open |
| D-095 | Database referential actions reach tenant tables on the owner connection, and an argument-inspecting guard cannot see them | RISK | 2026-08-03 | Found by the final C6 review. `identityDb.user.delete({ where: { id } })` names no relation, so `assertNoTenantRelation` passes it — and `memberships_userId_fkey ON DELETE CASCADE` then deletes that user's memberships across every organization, on the BYPASSRLS owner connection, with `can()` never consulted. Reproduced live: deleting one user silently removed two memberships in two different orgs. `Project`/`Assessment`/`Invitation` FKs to `users` are `RESTRICT`, so they are protected by accident rather than by design. **Fixed for now by removing `delete` and `deleteMany` from `EXPOSED_OPERATIONS`** — nothing in Plan 1a deletes users — but the *class* is open: any future operation whose effect is produced by the database rather than by the query arguments is invisible to a guard that inspects arguments | **When Plan 1b needs to delete a user**, add a named `deleteUser()` that tears down memberships explicitly through `withOrg`, rather than re-adding `delete` and relying on a referential action | Phase 1b | Open |
| D-096 | `requireDatabaseUrl` covers three PG* fields, not the PG* class — and `appClient` is exported, so `withOrg` is not the only path | RISK | 2026-08-03 | Two residuals from the final C6 review, kept together because they compose. (1) `pg` resolves `PG*` **per field**; the URL check covers `user`, `hostname` and `database` only, leaving `port`, `password`, `ssl` and — the security-relevant one — `options`. `PGOPTIONS='-c app.current_org_id=<org>'` pre-sets the isolation GUC on every pooled connection, so a query that never called `withOrg` returns that org's rows, inverting the fail-closed property T2 asserts. Mitigated by passing `options: ''` explicitly at all three `Pool` sites and rejecting an in-URL `options` parameter; `port`/`ssl` remain environment-resolvable and are not security-relevant here. (2) `appClient` is exported from `lib/data/tenant.ts`, so any module can query tenant tables without `withOrg` — on its own it fails closed (zero rows, which T2 asserts), but not in combination with (1) or after any session-scoped `set_config`. It is exported because two tests need it | **When Plan 1b ports its first route**: replace the `appClient` export with a test-only accessor and add it to the ESLint import ban, and decide whether `assertRestrictedIdentity` should move from `withOrg` to pool construction so it covers every path rather than one | Phase 1b | Open |
| D-097 | Invitation tokens are stored **plaintext**, where the design spec required a sha256 digest | BUG | 2026-08-03 | `prisma/schema.prisma` ships `token String @unique`; `docs/superpowers/specs/2026-08-02-phase1-foundation-design.md` §2.1 specifies `tokenHash` (sha256) with the raw token existing "only in the email", and also specifies an `acceptedAt` column that is likewise absent. The divergence was introduced in Plan 1a Task 1 and reviewed by nobody against the spec, because every review compared the diff to its own task brief rather than to the design. Consequence, stated concretely: **any read of the `invitations` table yields working capabilities** — a database backup, a log line, a `SELECT` by an operator, or an ORM bypass hands over the ability to join an organization at a role the row itself names. This is not hypothetical on this branch: the C6 review reproduced `invitationByToken(undefined)` returning a live pending invitation *with its token and `owner` role* (D-080), and the adversarial review separately reached live invitation tokens through `identityDb.user.$parent` (D-081). Hashing would not have prevented either bug; it would have made both leaks worthless. Note the raw token is still needed once — to build the delivered link — so the change is generate → hash → store digest → return the raw value to the sender exactly once | **Plan 1b's invitation work, before any invitation is created outside a test.** Migration adds `tokenHash` + `acceptedAt` and drops `token`; `preauth.invitationByToken` looks up by digest; assert in test that no stored value functions as a token (proof obligation O-7). Both databases hold zero invitation rows today, so the migration needs no backfill — a property that expires the moment a real invitation exists | Phase 1b | Open |
| D-098 | Invitation acceptance has no **email binding** designed, so whoever holds the link joins the organization | RISK | 2026-08-03 | Surfaced by the ADR-0002 threat model (STRIDE / Q3), and recorded now because the acceptance flow does not exist yet — this is a design obligation, not a shipped defect, and it is exactly the kind that is cheap now and expensive after the flow is written. `invitationByToken` authenticates the *token*, not the *person*. With email delivery the link is forwardable; with the copy-link fallback (D-030) it is pasteable into the wrong channel. Either way an invitation addressed to one person becomes organizational access for another, at the role the invitation names, and the resulting `Membership` is indistinguishable from a legitimate one. The binding must differ by case: for an email that already has an account, the accepting session's email must match; for a new account, the email is fixed from the invitation and not editable on the registration form. Neither rule is expressible in the schema, so both need tests. Interacts with ADR-0002 decision 9: never create a second `User` for an email that already has one, and never authenticate anyone as a side effect of acceptance | **The task that implements invitation acceptance.** Ships with tests for: forwarded link refused for a different authenticated user; invited email non-editable on the new-account form; `role` taken from the row and not the request body (O-5); replay creates no second membership (O-6) | Phase 1b | Open |
| D-099 | Per-request identity reads concentrate load on the **owner** connection, and the obvious performance fix re-opens D-075 | RISK | 2026-08-03 | Second-order consequence of ADR-0002 decisions 3–5, found by the `what-if-oracle` ∞ branch. Moving `role` and `mustChangePassword` out of the token means every authenticated request performs an identity read — and identity reads run on the owner connection (SUPERUSER, BYPASSRLS) because `makrai_app` deliberately holds no privilege on `users` (D-075). So **hardening session revocation increases traffic against the one pool in the system that bypasses tenant isolation**, currently `max: 5`. Under load the pool saturates and the natural-looking remedy is `GRANT SELECT ON users TO makrai_app`, which is precisely what D-075's own pick-up trigger predicts in writing: *"the temptation will be to re-grant the whole table."* The danger is the disguise — the pressure arrives as a latency graph, so the decision gets taken as a performance fix by whoever is on call, without the security review that a privilege grant would otherwise attract | **Any of: p95 latency on the owner pool, `identityDb` connection timeouts, or a proposal to grant `makrai_app` privilege on `users`.** Sanctioned responses in order: per-request memoisation (`cache()`), then pool sizing. If a grant ever becomes genuinely necessary it is column-scoped and excludes `passwordHash` (`GRANT SELECT (id, email, name, isActive, sessionEpoch) ON users`), never a bare table grant | Phase 1b | Open |
| D-100 | `docs/adr/README.md` indexed a **nonexistent** ADR-0002 as `Accepted`, and nothing checks cross-document references | BUG | 2026-08-03 | Found while deriving ADR-0002. The index on `main` linked `0002-identity-and-account-linking.md`, status `Accepted`; the file existed only on `rollback/phase1a-unadherent`. So the ADR index asserted the identity decision was settled while D-066 recorded that it was deliberately not derived — two project records in direct contradiction, with a reader meeting the wrong one first. Fixed in the same commit that adds the real ADR. **The instance is trivial; the class is not, and the class is the finding.** It survived a rollback, a full C6 phase-exit audit and a merge, because every mechanism this project has checks *code* against reality — `tsc`, ESLint, the test suite, `git status --untracked-files=all` — and not one checks whether a document's references resolve. A dangling link is invisible to all of them. This is also the documentation-deficit failure the product itself exists to diagnose (framework areas PO-03, PO-07), committed in our own repo, which AGENTS.md §6 says we do not get to do. Per §0.4, the fix is a mechanism and not a reminder | **Plan 1b's first documentation task.** Add a test that walks every relative markdown link under `docs/` and fails on a missing target, and wire it into `npm run verify`. Cheap (one test, no new dependency) and self-detecting thereafter | Phase 1b | Open |
| D-101 | Organization existence is disclosed by slug collision at creation, and the two 404 branches are timing-distinguishable | RISK | 2026-08-03 | Two residuals accepted by ADR-0002 decision 10, recorded rather than solved. (1) An org-creation form that reports *"that slug is taken"* is a plain existence oracle: it answers "does organization X use this platform?" for any guessed slug, defeating the 404 policy that the rest of the decision establishes. Server-side slug derivation (accept the display name, derive a unique slug, never report a collision) avoids it entirely and is the intended fix. (2) The two branches that both return 404 — "slug unknown" and "slug exists but caller is not a member" — perform different numbers of database queries and are therefore distinguishable by response time, which recovers the same information the status code withholds. Mitigated by always performing both lookups regardless of the first result. Severity is low in isolation and material in aggregate: for an assurance tool, *which institutions are running AI risk assessments* is competitively and reputationally sensitive, and it is the customer list | **The task that implements organization creation** (fix 1, server-side derivation) and **the task that implements `requireOrgContext`** (fix 2, unconditional double lookup). Neither is a follow-up: both are one-line-shaped decisions that are free at implementation time and awkward afterwards | Phase 1b | Open |
| D-102 | Playwright's Chromium does not launch on this machine, and Plan 1b's definition of done now depends on it | RISK | 2026-08-03 | Observed during Plan 1a's C5 verification: Playwright's bundled Chrome failed on a Qt platform-plugin error while the `chrome-devtools` MCP drove the same app successfully, so the work was verified live through the MCP instead and the incident was recorded **only** as a footnote in `docs/SKILLS_INVENTORY.md` §3 — never as a register row, because at the time an alternative launcher existed and the obligation was satisfied. That reasoning expired at the Plan 1b spec review: the human partner ruled the role × permission matrix must be **exhaustive live** as well as exhaustive at the integration level (2 orgs × 5 roles × 2 members, every role walking every screen), and a 20-user browser matrix driven through an MCP one call at a time is not a realistic substitute for a Playwright suite with `storageState` reuse and parallel workers. So a footnote about a tool that "has a workaround" has become a hard dependency of the phase exit. Note the shape of the failure this row exists to prevent: the launcher is needed at step 12, so the natural sequencing discovers it last, at the exact moment when the tempting response is to weaken the exit criterion rather than fix the tooling | **Step 0 of Plan 1b, before the fixture is built.** Timeboxed investigation with `superpowers:systematic-debugging` (D-084's lesson — one error message is a symptom, not a diagnosis; the Qt error most likely names a missing system library that `npx playwright install-deps` supplies). If Playwright cannot be made to launch, decide the verification vehicle explicitly and record it, rather than discovering the constraint at step 12 | Phase 1b | Open |

---

## Rollback annotation — 2026-08-02

Rows **D-055 through D-062** were opened by Phase-1a Tasks 1–5, whose commits were rolled back
to `9739454` on 2026-08-02 (see D-063). Their commit citations therefore point at objects that no
longer exist on `phase1a-isolation-spine`; the commits are preserved on branch
`rollback/phase1a-unadherent` and remain readable there.

Per rule 6 these rows are **retained, not deleted**. They are not automatically valid either: each
described a real consequence of an implementation that no longer exists, so each must be
re-evaluated during the redo and either re-opened against the new commits, or closed with a note
that the redo did not reproduce the condition. Treating them as still-true would be exactly the
transcription error rule 8 forbids.

---

## Closure log

Closed rows keep their register entry above (status updated) and gain an entry here with
evidence. Nothing is deleted.

**D-041** — Closed 2026-08-02. `lib/data/identity.ts` was introduced in `ceece2d` (Task 4) as
the documented boundary for `User`/`ConsentRecord`, satisfying the row's letter. Fix-round-1
review of that commit found the boundary was documentation-only: `identityDb` connects as
`makrai`, verified live to be a Postgres superuser with `rolbypassrls = t` (`SELECT rolsuper,
rolbypassrls FROM pg_roles WHERE rolname='makrai'` → `t|t`), so no RLS policy from Task 5 could
ever have constrained it — `identityDb.project.findMany()` type-checked and would have returned
every tenant's rows. `fad9627` closes the gap by narrowing `identityDb`'s exported type to
`Omit<PrismaClient, 'project' | 'projectMetadata' | 'assessment' | 'remediationItem' |
'organization' | 'membership' | 'invitation'>`, verified live in two directions: (1) with the
fix in place, `tsc --noEmit` reports exactly the 3 known/unrelated errors and
`__tests__/integration/identity-boundary.test.ts`'s 7 `@ts-expect-error` assertions all
type-check as genuine errors; (2) with the fix temporarily reverted (`identityDb: PrismaClient`
instead of `NonTenantClient`), the same 7 assertions immediately became "Unused
`@ts-expect-error` directive" errors (10 total), proving the guard actually fires rather than
passing vacuously, before the fix was restored. The boundary is now compiler-enforced, not
documentation-only.

**D-041 — closure evidence re-anchored 2026-08-03 (Task 4 redo).** The paragraph above is
retained unedited because nothing here is deleted, but two of its load-bearing citations no
longer resolve on this branch and a reader would otherwise trust them. `ceece2d` and `fad9627`
are commits of the **rolled-back** attempt: `git merge-base --is-ancestor <sha> HEAD` reports
neither is an ancestor of `HEAD`, so the code they describe was not present in the tree when
this row was read at Task 4 start (`ls lib/data` → "No such file or directory"). The
`__tests__/integration/identity-boundary.test.ts` it cites does not exist on this branch
either. The row's *substance* is nonetheless satisfied, by the Task 4 commit that carries this
paragraph: `lib/data/identity.ts` now exists as the documented `User`/`ConsentRecord` boundary,
and the boundary is compiler-enforced by an **allowlist** — `Pick<PrismaClient, 'user' |
'consentRecord'>` — rather than the `Omit<...>` denylist described above. The allowlist is
strictly stronger and was chosen because `Omit` left three holes the earlier text did not
account for: `$queryRaw*`/`$executeRaw*` survived it, `$transaction`'s handle was typed with
the full model set, and any tenant model added later was admitted by default. Verified live by
four `tsc --noEmit` probes, the fourth a negative control: `identityDb.project.findMany()` →
`Property 'project' does not exist on type 'NonTenantClient'`; `identityDb.$queryRaw` →
`Property '$queryRaw' does not exist`; `identityDb.$transaction(...)` → `Property
'$transaction' does not exist`; `identityDb.user.findMany()` → **no error**, i.e. the allowlist
still permits what it exists to serve. Status stays `Closed-done`.

**D-060** — Closed 2026-08-03 by the Task 4 commit that carries this entry. The row recorded
that the Task-4 brief's client code omitted the HMR-safe singleton guard, so every Next.js dev
hot-reload would leak a `PrismaClient` and its `pg.Pool` until the server exhausted
connections. All three clients created by this task now carry the guard, mirroring the existing
pattern read directly at `lib/db.ts:5-17` rather than recalled: `lib/data/tenant.ts` guards
`appClient` via `globalForData`, `lib/data/identity.ts` guards `identityDb` via
`globalForIdentity`, and `lib/data/preauth.ts` guards its module-private `ownerClient` via
`globalForPreauth` — each assigning to the `globalThis` slot only when
`process.env.NODE_ENV !== 'production'`, exactly as `lib/db.ts` does. Pool sizes are also
pinned rather than defaulted (`max: 10` for the app client, `max: 5` each for identity and
preauth), because four default pools would reserve 40 of the server's connections;
`SHOW max_connections` was re-run live at execution time rather than inherited from the brief's
comment and returned **100**, with 6 in use. **What was NOT verified live:** this closure rests
on source inspection and pool arithmetic, not on an observed dev-mode hot-reload — no route
imports any of the three clients yet (Plan 1b wires them), so there is currently no dev code
path on which HMR leakage could be demonstrated or refuted. The guard is verified present and
correct by reading; it is not verified effective by exercise.

---

## Review cadence

- **Every phase exit:** full register review. No phase exits with an `Open` row whose target
  is that phase, unless the row is explicitly re-targeted with justification.
- **Every spec written under `superpowers:brainstorming`:** its Deferrals section lands here
  in the same commit.
- **Every `what-if-oracle` run:** its decision triggers become rows if they imply deferred work.
